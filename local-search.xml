<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Java之基础学习（三）面向对象</title>
    <link href="undefined2019/11/13/java%E4%B9%8B%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0%EF%BC%88%E4%B8%89%EF%BC%89%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1/"/>
    <url>2019/11/13/java%E4%B9%8B%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0%EF%BC%88%E4%B8%89%EF%BC%89%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><h3 id="面向对象："><a href="#面向对象：" class="headerlink" title="面向对象："></a>面向对象：</h3><h4 id="特点："><a href="#特点：" class="headerlink" title="特点："></a>特点：</h4><ul><li>1：将复杂的事情简单化。</li><li>2：面向对象将以前的过程中的执行者，变成了指挥者。</li><li>3：面向对象这种思想是符合现在人们思考习惯的一种思想。</li></ul><p>过程和对象在我们的程序中是如何体现的呢？过程其实就是函数；对象是将函数等一些内容进行了封装。</p><p>匿名对象使用场景：</p><ul><li>1：当对方法只进行一次调用的时候，可以使用匿名对象。</li><li>2：当对象对成员进行多次调用时，不能使用匿名对象。必须给对象起名字。</li></ul><p>在类中定义其实都称之为成员。成员有两种：</p><ul><li>1：成员变量：其实对应的就是事物的属性。</li><li>2：成员函数：其实对应的就是事物的行为。</li></ul><p>所以，其实定义类，就是在定义成员变量和成员函数。但是在定义前，必须先要对事物进行属性和行为的分析，才可以用代码来体现。</p><p><code>private int age;//私有的访问权限最低，只有在本类中的访问有效。</code></p><p>注意：私有仅仅是封装的一种体现形式而已。</p><p>私有的成员：其他类不能直接创建对象访问，所以只有通过本类对外提供具体的访问方式来完成对私有的访问，可以通过对外提供函数的形式对其进行访问。</p><p>好处：可以在函数中加入逻辑判断等操作，对数据进行判断等操作。</p><p>总结：开发时，记住，属性是用于存储数据的，直接被访问，容易出现安全隐患，所以，类中的属性通常被私有化，并对外提供公共的访问方法。</p><p>这个方法一般有两个，规范写法：对于属性 xxx，可以使用setXXX(),getXXX()对其进行操作。</p><p>类中怎么没有定义主函数呢？</p><p>注意：主函数的存在，仅为该类是否需要独立运行，如果不需要，主函数是不用定义的。</p><p>主函数的解释：保证所在类的独立运行，是程序的入口，被jvm调用。</p><p>成员变量和局部变量的区别：</p><p>1：成员变量直接定义在类中。<br>局部变量定义在方法中，参数上，语句中。</p><p>2：成员变量在这个类中有效。<br>局部变量只在自己所属的大括号内有效，大括号结束，局部变量失去作用域。</p><p>3：成员变量存在于堆内存中，随着对象的产生而存在，消失而消失。<br>局部变量存在于栈内存中，随着所属区域的运行而存在，结束而释放。</p><h4 id="构造函数：用于给对象进行初始化，是给与之对应的对象进行初始化，它具有针对性，函数中的一种。"><a href="#构造函数：用于给对象进行初始化，是给与之对应的对象进行初始化，它具有针对性，函数中的一种。" class="headerlink" title="构造函数：用于给对象进行初始化，是给与之对应的对象进行初始化，它具有针对性，函数中的一种。"></a>构造函数：用于给对象进行初始化，是给与之对应的对象进行初始化，它具有针对性，函数中的一种。</h4><p>特点：</p><ul><li>1：该函数的名称和所在类的名称相同。</li><li>2：不需要定义返回值类型。</li><li>3：该函数没有具体的返回值。</li></ul><p>记住：所有对象创建时，都需要初始化才可以使用。</p><p>注意事项：一个类在定义时，如果没有定义过构造函数，那么该类中会自动生成一个空参数的构造函数，为了方便该类创建对象，完成初始化。如果在类中自定义了构造函数，那么默认的构造函数就没有了。</p><p>一个类中，可以有多个构造函数，因为它们的函数名称都相同，所以只能通过参数列表来区分。所以，一个类中如果出现多个构造函数。它们的存在是以重载体现的。</p><p>构造函数和一般函数有什么区别呢？</p><ul><li>1：两个函数定义格式不同。</li><li>2：构造函数是在对象创建时，就被调用，用于初始化，而且初始化动作只执行一次。<br>一般函数，是对象创建后，需要调用才执行，可以被调用多次。</li></ul><p>什么时候使用构造函数呢？</p><p>分析事物时，发现具体事物一出现，就具备了一些特征，那就将这些特征定义到构造函数内。</p><p>构造代码块和构造函数有什么区别？</p><p>构造代码块：是给所有的对象进行初始化，也就是说，所有的对象都会调用一个代码块。只要对象一建立。就会调用这个代码块。</p><p>构造函数：是给与之对应的对象进行初始化。它具有针对性。</p><p><code>Person p = new Person();</code></p><p>创建一个对象都在内存中做了什么事情？</p><ul><li>1：先将硬盘上指定位置的Person.class文件加载进内存。</li><li>2：执行main方法时，在栈内存中开辟了main方法的空间(压栈-进栈)，然后在main方法的栈区分配了一个变量p。</li><li>3：在堆内存中开辟一个实体空间，分配了一个内存首地址值。new</li><li>4：在该实体空间中进行属性的空间分配，并进行了默认初始化。</li><li>5：对空间中的属性进行显示初始化。</li><li>6：进行实体的构造代码块初始化。</li><li>7：调用该实体对应的构造函数，进行构造函数初始化。（）</li><li>8：将首地址赋值给p ，p变量就引用了该实体。(指向了该对象)</li></ul><hr><h4 id="封-装（面向对象特征之一）：是指隐藏对象的属性和实现细节，仅对外提供公共访问方式。"><a href="#封-装（面向对象特征之一）：是指隐藏对象的属性和实现细节，仅对外提供公共访问方式。" class="headerlink" title="封 装（面向对象特征之一）：是指隐藏对象的属性和实现细节，仅对外提供公共访问方式。"></a>封 装（面向对象特征之一）：是指隐藏对象的属性和实现细节，仅对外提供公共访问方式。</h4><p>好处：将变化隔离；便于使用；提高重用性；安全性。</p><p>封装原则：将不需要对外提供的内容都隐藏起来，把属性都隐藏，提供公共方法对其访问。</p><p>this:代表对象。就是所在函数所属对象的引用。</p><p>this到底代表什么呢？哪个对象调用了this所在的函数，this就代表哪个对象，就是哪个对象的引用。</p><p>开发时，什么时候使用this呢？</p><p>在定义功能时，如果该功能内部使用到了调用该功能的对象，这时就用this来表示这个对象。</p><p>this 还可以用于构造函数间的调用。</p><p>调用格式：this(实际参数)；</p><p>this对象后面跟上 . 调用的是成员属性和成员方法(一般方法)；</p><p>this对象后面跟上 () 调用的是本类中的对应参数的构造函数。</p><p>注意：用this调用构造函数，必须定义在构造函数的第一行。因为构造函数是用于初始化的，所以初始化动作一定要执行。否则编译失败。</p><h4 id="static关键字，是一个修饰符，用于修饰成员-成员变量和成员函数-。"><a href="#static关键字，是一个修饰符，用于修饰成员-成员变量和成员函数-。" class="headerlink" title="static关键字，是一个修饰符，用于修饰成员(成员变量和成员函数)。"></a><code>static</code>关键字，是一个修饰符，用于修饰成员(成员变量和成员函数)。</h4><p>特点：</p><ul><li>1，想要实现对象中的共性数据的对象共享。可以将这个数据进行静态修饰。</li><li>2，被静态修饰的成员，可以直接被类名所调用。也就是说，静态的成员多了一种调用方式。类名.静态方式。</li><li>3，静态随着类的加载而加载。而且优先于对象存在。</li></ul><p>弊端：</p><ul><li>1，有些数据是对象特有的数据，是不可以被静态修饰的。因为那样的话，特有数据会变成对象的共享数据。这样对事物的描述就出了问题。所以，在定义静态时，必须要明确，这个数据是否是被对象所共享的。</li><li>2，静态方法只能访问静态成员，不可以访问非静态成员。<br>因为静态方法加载时，优先于对象存在，所以没有办法访问对象中的成员。</li><li>3，静态方法中不能使用this，super关键字。<br>因为this代表对象，而静态在时，有可能没有对象，所以this无法使用。</li><li>4，主函数是静态的。</li></ul><p>什么时候定义静态成员呢？或者说：定义成员时，到底需不需要被静态修饰呢？</p><p>成员分两种：</p><ul><li>1，成员变量。（数据共享时静态化）<br>该成员变量的数据是否是所有对象都一样：<br>如果是，那么该变量需要被静态修饰，因为是共享的数据。<br>如果不是，那么就说这是对象的特有数据，要存储到对象中。</li><li>2，成员函数。（方法中没有调用特有数据时就定义成静态）<br>如果判断成员函数是否需要被静态修饰呢？<br>只要参考，该函数内是否访问了对象中的特有数据：<br>如果有访问特有数据，那方法不能被静态修饰。<br>如果没有访问过特有数据，那么这个方法需要被静态修饰。</li></ul><p>成员变量和静态变量的区别：</p><ul><li>1，成员变量所属于对象。所以也称为实例变量。<br>静态变量所属于类。所以也称为类变量。</li><li>2，成员变量存在于堆内存中。<br>静态变量存在于方法区中。</li><li>3，成员变量随着对象创建而存在。随着对象被回收而消失。<br>静态变量随着类的加载而存在。随着类的消失而消失。</li><li>4，成员变量只能被对象所调用 。</li></ul><p>静态变量可以被对象调用，也可以被类名调用。<br>所以，成员变量可以称为对象的特有数据，静态变量称为对象的共享数据。</p><p>静态的注意：静态的生命周期很长。</p><p>静态代码块：就是一个有静态关键字标示的一个代码块区域。定义在类中。</p><p>作用：可以完成类的初始化。静态代码块随着类的加载而执行，而且只执行一次（new 多个对象就只执行一次）。如果和主函数在同一类中，优先于主函数执行。</p><p>Public：访问权限最大。</p><p>static：不需要对象，直接类名即可。</p><p>void：主函数没有返回值。</p><p>Main：主函数特定的名称。</p><p>(String[] args)：主函数的参数，是一个字符串数组类型的参数，jvm调用main方法时，传递的实际参数是 new String[0]。</p><p>jvm默认传递的是长度为0的字符串数组，我们在运行该类时，也可以指定具体的参数进行传递。可以在控制台，运行该类时，在后面加入参数。参数之间通过空格隔开。jvm会自动将这些字符串参数作为args数组中的元素，进行存储。</p><p>静态代码块、构造代码块、构造函数同时存在时的执行顺序：静态代码块 à 构造代码块 à 构造函数；</p><p>生成Java帮助文档：</p><p>命令格式：javadoc –d 文件夹名 –auther –version *.java</p><pre><code class="java">/**     //格式*类描述*@author 作者名*@version 版本号*//***方法描述*@param  参数描述*@return  返回值描述*/</code></pre><hr><h3 id="继-承（面向对象特征之一）"><a href="#继-承（面向对象特征之一）" class="headerlink" title="继 承（面向对象特征之一）"></a>继 承（面向对象特征之一）</h3><p>好处：</p><ul><li>1：提高了代码的复用性。</li><li>2：让类与类之间产生了关系，提供了另一个特征多态的前提。</li></ul><p>父类的由来：其实是由多个类不断向上抽取共性内容而来的。</p><p>java中对于继承，java只支持单继承。java虽然不直接支持多继承，但是保留了这种多继承机制，进行改良。</p><p>单继承：一个类只能有一个父类。</p><p>多继承：一个类可以有多个父类。</p><p>为什么不支持多继承呢？</p><p>因为当一个类同时继承两个父类时，两个父类中有相同的功能，那么子类对象调用该功能时，运行哪一个呢？因为父类中的方法中存在方法体。</p><p>但是java支持多重继承。A继承B B继承C C继承D。</p><p>多重继承的出现，就有了继承体系。体系中的顶层父类是通过不断向上抽取而来的。它里面定义的该体系最基本最共性内容的功能。</p><p>所以，一个体系要想被使用，直接查阅该系统中的父类的功能即可知道该体系的基本用法。那么想要使用一个体系时，需要建立对象。建议建立最子类对象，因为最子类不仅可以使用父类中的功能。还可以使用子类特有的一些功能。</p><p>简单说：对于一个继承体系的使用，查阅顶层父类中的内容，创建最底层子类的对象。</p><p>子父类出现后，类中的成员都有了哪些特点：</p><ul><li>1：成员变量。<br>当子父类中出现一样的属性时，子类类型的对象，调用该属性，值是子类的属性值。<br>如果想要调用父类中的属性值，需要使用一个关键字：super<br>This：代表是本类类型的对象引用。<br>Super：代表是子类所属的父类中的内存空间引用。<br>注意：子父类中通常是不会出现同名成员变量的，因为父类中只要定义了，子类就不用在定义了，直接继承过来用就可以了。</li><li>2：成员函数。<br>当子父类中出现了一模一样的方法时，建立子类对象会运行子类中的方法。好像父类中的方法被覆盖掉一样。所以这种情况，是函数的另一个特性：覆盖(复写，重写)<br>什么时候使用覆盖呢？当一个类的功能内容需要修改时，可以通过覆盖来实现。</li><li>3：构造函数。<br>发现子类构造函数运行时，先运行了父类的构造函数。为什么呢?<br>原因：子类的所有构造函数中的第一行，其实都有一条隐身的语句<code>super()</code>;<br><code>super()</code>: 表示父类的构造函数，并会调用于参数相对应的父类中的构造函数。而<code>super()</code>:是在调用父类中空参数的构造函数。<br>为什么子类对象初始化时，都需要调用父类中的函数？(为什么要在子类构造函数的第一行加入这个super()?)<br>因为子类继承父类，会继承到父类中的数据，所以必须要看父类是如何对自己的数据进行初始化的。所以子类在进行对象初始化时，先调用父类的构造函数，这就是子类的实例化过程。</li></ul><p>注意：子类中所有的构造函数都会默认访问父类中的空参数的构造函数，因为每一个子类构造内第一行都有默认的语句super();<br>如果父类中没有空参数的构造函数，那么子类的构造函数内，必须通过super语句指定要访问的父类中的构造函数。<br>如果子类构造函数中用this来指定调用子类自己的构造函数，那么被调用的构造函数也一样会访问父类中的构造函数。</p><p>问题：<code>super()</code>和<code>this()</code>是否可以同时出现的构造函数中。</p><p>两个语句只能有一个定义在第一行，所以只能出现其中一个。</p><p><code>super()</code>或者<code>this()</code>:为什么一定要定义在第一行？</p><p>因为<code>super()</code>或者<code>this()</code>都是调用构造函数，构造函数用于初始化，所以初始化的动作要先完成。</p><p>继承的细节：</p><p>什么时候使用继承呢？</p><p>当类与类之间存在着所属关系时，才具备了继承的前提。a是b中的一种。a继承b。狼是犬科中的一种。</p><p>英文书中，所属关系：” is a “</p><p>注意：不要仅仅为了获取其他类中的已有成员进行继承。</p><p>所以判断所属关系，可以简单看，如果继承后，被继承的类中的功能，都可以被该子类所具备，那么继承成立。如果不是，不可以继承。</p><p>细节二：</p><p>在方法覆盖时，注意两点：</p><ul><li>1：子类覆盖父类时，必须要保证，子类方法的权限必须大于等于父类方法权限可以实现继承。否则，编译失败。</li><li>2：覆盖时，要么都静态，要么都不静态。 (静态只能覆盖静态，或者被静态覆盖)</li></ul><p>继承的一个弊端：打破了封装性。对于一些类，或者类中功能，是需要被继承，或者复写的。</p><p>这时如何解决问题呢？介绍一个关键字，final:最终。</p><p>final特点：</p><ul><li>1：这个关键字是一个修饰符，可以修饰类，方法，变量。</li><li>2：被final修饰的类是一个最终类，不可以被继承。</li><li>3：被final修饰的方法是一个最终方法，不可以被覆盖。</li><li>4：被final修饰的变量是一个常量，只能赋值一次。</li></ul><p>其实这样的原因的就是给一些固定的数据起个阅读性较强的名称。</p><p>不加final修饰不是也可以使用吗？那么这个值是一个变量，是可以更改的。加了final，程序更为严谨。常量名称定义时，有规范，所有字母都大写，如果由多个单词组成，中间用 _ 连接。</p><blockquote><p>业精于勤，荒于嬉；行成于思，毁于随。——韩愈</p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java之基础学习（二）java语法基础</title>
    <link href="undefined2019/11/13/java%E4%B9%8B%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0%EF%BC%88%E4%BA%8C%EF%BC%89java%E8%AF%AD%E6%B3%95%E5%9F%BA%E7%A1%80/"/>
    <url>2019/11/13/java%E4%B9%8B%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0%EF%BC%88%E4%BA%8C%EF%BC%89java%E8%AF%AD%E6%B3%95%E5%9F%BA%E7%A1%80/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><ul><li><p>1，关键字：其实就是某种语言赋予了特殊含义的单词。<br>保留字：其实就是还没有赋予特殊含义，但是准备日后要使用过的单词。</p></li><li><p>2，标示符：其实就是在程序中自定义的名词。比如类名，变量名，函数名。包含 0-9、a-z、$、_ ；<br>注意：</p><p>1），数字不可以开头。</p><p>2），不可以使用关键字。</p></li><li><p>3，常量：是在程序中的不会变化的数据。</p></li><li><p>4，变量：其实就是内存中的一个存储空间，用于存储常量数据。<br>作用：方便于运算。因为有些数据不确定。所以确定该数据的名词和存储空间。<br>特点：变量空间可以重复使用。</p></li></ul><p>什么时候定义变量？只要是数据不确定的时候，就定义变量。</p><p>变量空间的开辟需要什么要素呢？</p><ul><li>1，这个空间要存储什么数据？数据类型。</li><li>2，这个空间叫什么名字啊？变量名称。</li><li>3，这个空间的第一次的数据是什么？ 变量的初始化值。</li></ul><p>变量的作用域和生存期:</p><p>变量的作用域：</p><pre><code>作用域从变量定义的位置开始，到该变量所在的那对大括号结束；</code></pre><p>生命周期：</p><pre><code>变量从定义的位置开始就在内存中活了；变量到达它所在的作用域的时候就在内存中消失了；</code></pre><p>数据类型：</p><pre><code>1）：基本数据类型：byte、short、int、long、float、double、char、boolean2）：引用数据类型: 数组、类、接口。</code></pre><p>级别从低到高为：byte,char,short(这三个平级)–&gt;int–&gt;float–&gt;long–&gt;double</p><p>自动类型转换：从低级别到高级别，系统自动转的；</p><p>强制类型转换：什么情况下使用?把一个高级别的数赋给一个别该数的级别低的变量；</p><p>运算符号：</p><pre><code>1）、算术运算符。    + - * / %   %:任何整数模2不是0就是1，所以只要改变被模数就可以实现开关运算。    +:连接符。    ++,--2）、赋值运算符。    =  += -= *= /= %=3）、比较运算符。    特点：该运算符的特点是：运算完的结果，要么是true，要么是false。4）、逻辑运算符。    &amp;  |  ^  !   &amp;&amp;   ||    逻辑运算符除了 !  外都是用于连接两个boolean类型表达式。    &amp;: 只有两边都为true结果是true。否则就是false。    |:只要两边都为false结果是false，否则就是true    ^:异或：和或有点不一样。         两边结果一样，就为false。         两边结果不一样，就为true.    &amp; 和 &amp;&amp;区别： &amp; ：无论左边结果是什么，右边都参与运算。                  &amp;&amp;:短路与，如果左边为false，那么右边不参数与运算。    | 和|| 区别：|：两边都运算。                ||：短路或，如果左边为true，那么右边不参与运算。5）、位运算符:用于操作二进制位的运算符。    &amp;  |  ^    &lt;&lt;  &gt;&gt;   &gt;&gt;&gt;(无符号右移)练习：对两个变量的数据进行互换。不需要第三方变量。        int a  = 3,b = 5;--&gt;b = 3,a = 5;        a = a + b; a = 8;        b = a - b; b = 3;        a = a - b; a = 5;        a = a ^ b;//        b = a ^ b;//b = a ^ b ^ b = a        a = a ^ b;//a = a ^ b ^ a = b;    练习：高效的算出 2*8 = 2&lt;&lt;3;</code></pre><ul><li><p>5，语句。</p><p>If switch do while while for</p><p>这些语句什么时候用？</p><p>1）、当判断固定个数的值的时候，可以使用if，也可以使用switch。</p><p>但是建议使用switch，效率相对较高。</p></li></ul><pre><code class="java">switch(变量){   case 值:要执行的语句;break;   …   default:要执行的语句;}</code></pre><p>工作原理：用小括号中的变量的值依次和case后面的值进行对比，和哪个case后面的值相同了<br>就执行哪个case后面的语句，如果没有相同的则执行default后面的语句；</p><p>细节：</p><pre><code>1）：break是可以省略的，如果省略了就一直执行到遇到break为止；2）：switch 后面的小括号中的变量应该是byte,char,short,int四种类型中的一种；3）：default可以写在switch结构中的任意位置；如果将default语句放在了第一行，则不管expression与case中的value是否匹配，程序会从default开始执行直到第一个break出现。4）、当判断数据范围，获取判断运算结果boolean类型时，需要使用if。5）、当某些语句需要执行很多次时，就用循环结构。    while和for可以进行互换。    区别在于：如果需要定义变量控制循环次数。建议使用for。因为for循环完毕，变量在内存中释放。</code></pre><p>break:作用于switch ，和循环语句，用于跳出，或者称为结束。</p><p>break语句单独存在时，下面不要定义其他语句，因为执行不到，编译会失败。当循环嵌套时，break只跳出当前所在循环。要跳出嵌套中的外部循环，只要给循环起名字即可，这个名字称之为标号。</p><p>continue:只作用于循环结构，继续循环用的。</p><p>作用：结束本次循环，继续下次循环。该语句单独存在时，下面不可以定义语句，执行不到。</p><p>6，函 数：为了提高代码的复用性，可以将其定义成一个单独的功能，该功能的体现就是java中的函数。函数就是体现之一。</p><p>java中的函数的定义格式：</p><pre><code>修饰符 返回值类型 函数名(参数类型 形式参数1，参数类型 形式参数1，…){    执行语句；    return 返回值；}</code></pre><p>当函数没有具体的返回值时，返回的返回值类型用void关键字表示。</p><p>如果函数的返回值类型是void时，return语句可以省略不写的，系统会帮你自动加上。</p><p>return的作用：结束函数。结束功能。</p><p>如何定义一个函数？</p><pre><code>函数其实就是一个功能，定义函数就是实现功能，通过两个明确来完成：1）、明确该功能的运算完的结果，其实是在明确这个函数的返回值类型。2）、在实现该功能的过程中是否有未知内容参与了运算，其实就是在明确这个函数的参数列表(参数类型&amp;参数个数)。</code></pre><p>函数的作用：</p><pre><code>1）、用于定义功能。2）、用于封装代码提高代码的复用性。</code></pre><p>注意：函数中只能调用函数，不能定义函数。</p><pre><code>主函数：    1）、保证该类的独立运行。    2）、因为它是程序的入口。    3）、因为它在被jvm调用。</code></pre><p>函数定义名称是为什么呢？</p><pre><code>答：1）、为了对该功能进行标示，方便于调用。    2）、为了通过名称就可以明确函数的功能，为了增加代码的阅读性。</code></pre><p>重载的定义是：在一个类中，如果出现了两个或者两个以上的同名函数，只要它们的参数的个数，或者参数的类型不同，即可称之为该函数重载了。</p><p>如何区分重载：当函数同名时，只看参数列表。和返回值类型没关系。</p><p>7，数 组：用于存储同一类型数据的一个容器。好处：可以对该容器中的数据进行编号，从0开始。数组用于封装数据，就是一个具体的实体。</p><p>如何在java中表现一个数组呢？两种表现形式。</p><pre><code>1）、元素类型[] 变量名 = new 元素类型[元素的个数]；2）、元素类型[] 变量名 = {元素1，元素2...}；元素类型[] 变量名 = new 元素类型[]{元素1，元素2...}；</code></pre><hr><p>二分查找法。必须有前提：数组中的元素要有序。</p><pre><code class="java">public static int halfSeach_2(int[] arr,int key){        int min,max,mid;        min = 0;        max = arr.length-1;        mid = (max+min)&gt;&gt;1; //(max+min)/2;        while(arr[mid]!=key){            if(key&gt;arr[mid]){                min = mid + 1;            }            else if(key&lt;arr[mid])                max = mid - 1;            if(max&lt;min)                return -1;            mid = (max+min)&gt;&gt;1;        }        return mid;    }</code></pre><hr><p>java分了5片内存。</p><p>1：寄存器。2：本地方法区。3：方法区。4：栈。5：堆。</p><p>栈：存储的都是局部变量 ( 函数中定义的变量，函数上的参数，语句中的变量 )；</p><pre><code>只要数据运算完成所在的区域结束，该数据就会被释放。</code></pre><p>堆：用于存储数组和对象，也就是实体。啥是实体啊？就是用于封装多个数据的。</p><p>1：每一个实体都有内存首地址值。</p><p>2：堆内存中的变量都有默认初始化值。因为数据类型不同，值也不一样。</p><p>3：垃圾回收机制。</p><blockquote><p>知人者智，自知者明。胜人者有力，自胜者强。——老子</p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>java之基础学习（一）Java概述</title>
    <link href="undefined2019/11/13/java%E4%B9%8B%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0%EF%BC%88%E4%B8%80%EF%BC%89Java%E6%A6%82%E8%BF%B0/"/>
    <url>2019/11/13/java%E4%B9%8B%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0%EF%BC%88%E4%B8%80%EF%BC%89Java%E6%A6%82%E8%BF%B0/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><h3 id="写代码："><a href="#写代码：" class="headerlink" title="写代码："></a>写代码：</h3><ul><li>1，明确需求。我要做什么？</li><li>2，分析思路。我要怎么做？1,2,3。</li><li>3，确定步骤。每一个思路部分用到哪些语句，方法，和对象。</li><li>4，代码实现。用具体的java语言代码把思路体现出来。</li></ul><h3 id="学习新技术的四点："><a href="#学习新技术的四点：" class="headerlink" title="学习新技术的四点："></a>学习新技术的四点：</h3><ul><li>1，该技术是什么？</li><li>2，该技术有什么特点(使用注意)：</li><li>3，该技术怎么使用。demo</li><li>4，该技术什么时候用？test。<h3 id="一：java概述："><a href="#一：java概述：" class="headerlink" title="一：java概述："></a>一：java概述：</h3>1991 年Sun公司的James Gosling等人开始开发名称为 Oak 的语言，希望用于控制嵌入在有线电视交换盒、PDA等的微处理器；<br>1994年将Oak语言更名为Java；</li></ul><h3 id="Java的三种技术架构"><a href="#Java的三种技术架构" class="headerlink" title="Java的三种技术架构:"></a>Java的三种技术架构:</h3><ul><li>JAVAEE：Java Platform Enterprise Edition，开发企业环境下的应用程序，主要针对web程序开发；</li><li>JAVASE：Java Platform Standard Edition，完成桌面应用程序的开发，是其它两者的基础；</li><li>JAVAME：Java Platform Micro Edition，开发电子消费产品和嵌入式设备，如手机中的程序；</li></ul><ol><li>JDK：Java Development Kit，java的开发和运行环境，java的开发工具和jre。</li><li>JRE：Java Runtime Environment，java程序的运行环境，java运行的所需的类库+JVM(java虚拟机)。</li><li>配置环境变量：让java jdk\bin目录下的工具，可以在任意目录下运行，原因是，将该工具所在目录告诉了系统，当使用该工具时，由系统帮我们去找指定的目录。</li></ol><h4 id="环境变量的配置："><a href="#环境变量的配置：" class="headerlink" title="环境变量的配置："></a><strong>环境变量的配置：</strong></h4><pre><code class="shell">    1）：永久配置方式：JAVA_HOME=%安装路径%\Java\jdk                      path=%JAVA_HOME%\bin    2）：临时配置方式：set path=%path%;C:\Program Files\Java\jdk\bin</code></pre><p>特点：系统默认先去当前路径下找要执行的程序，如果没有，再去path中设置的路径下找。</p><h3 id="classpath的配置"><a href="#classpath的配置" class="headerlink" title="classpath的配置:"></a><strong>classpath的配置:</strong></h3><pre><code class="shell">    1）：永久配置方式：classpath=.;c:\;e:\    2）：临时配置方式：set classpath=.;c:\;e:\</code></pre><p>注意：在定义<code>classpath</code>环境变量时，需要注意的情况<br>如果没有定义环境变量<code>classpath</code>，<code>java</code>启动<code>jvm</code>后，会在当前目录下查找要运行的类文件；<br>如果指定了<code>classpath</code>，那么会在指定的目录下查找要运行的类文件。<br>还会在当前目录找吗？两种情况：</p><ol><li>如果classpath的值结尾处有分号，在具体路径中没有找到运行的类，会默认在当前目录再找一次。</li><li>如果classpath的值结果出没有分号，在具体的路径中没有找到运行的类，不会再当前目录找。<br>一般不指定分号，如果没有在指定目录下找到要运行的类文件，就报错，这样可以调试程序。</li></ol><h3 id="javac命令和java命令做什么事情呢？"><a href="#javac命令和java命令做什么事情呢？" class="headerlink" title="javac命令和java命令做什么事情呢？"></a>javac命令和java命令做什么事情呢？</h3><p>要知道java是分两部分的：一个是编译，一个是运行。</p><ul><li>javac：负责的是编译的部分，当执行javac时，会启动java的编译器程序。对指定扩展名的.java文件进行编译。 生成了jvm可以识别的字节码文件。也就是class文件，也就是java的运行程序。</li><li>java：负责运行的部分.会启动jvm.加载运行时所需的类库,并对class文件进行执行.</li><li>一个文件要被执行,必须要有一个执行的起始点,这个起始点就是main函数.</li></ul><blockquote><p>有时候读书是一种巧妙地避开思考的方法。——赫尔普斯</p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Spring Boot 配置多环境Logback-spring.xml</title>
    <link href="undefined2019/11/13/spring%20boot%20%E9%85%8D%E7%BD%AE%E5%A4%9A%E7%8E%AF%E5%A2%83logback-spring.xml/"/>
    <url>2019/11/13/spring%20boot%20%E9%85%8D%E7%BD%AE%E5%A4%9A%E7%8E%AF%E5%A2%83logback-spring.xml/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="/img/1.jpeg" srcset="undefined" alt=""></p><p>记录一下spring boot 配置日志记录：</p><p>在SpringBoot新建 <code>logback-spring.xml</code> 配置文件，因为<code>Spring Boot</code>官方是推荐这个方式:</p><p>格式如下：</p><pre><code class="xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;!-- 日志级别从低到高分为TRACE &lt; DEBUG &lt; INFO &lt; WARN &lt; ERROR &lt; FATAL，如果设置为WARN，则低于WARN的信息都不会输出 --&gt;&lt;!-- scan:当此属性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true --&gt;&lt;!-- scanPeriod:设置监测配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的时间间隔为1分钟。 --&gt;&lt;!-- debug:当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。 --&gt;&lt;configuration scan=&quot;true&quot; scanPeriod=&quot;10 seconds&quot;&gt;    &lt;!-- name的值是变量的名称，value的值时变量定义的值。通过定义的值会被插入到logger上下文中。定义变量后，可以使“${}”来使用变量。 --&gt;    &lt;springProperty scope=&quot;context&quot; name=&quot;logDir&quot; source=&quot;logging.path&quot;/&gt;    &lt;springProperty scope=&quot;context&quot; name=&quot;service&quot; source=&quot;spring.application.name&quot;/&gt;    &lt;!-- 彩色日志 --&gt;    &lt;!-- 彩色日志依赖的渲染类 --&gt;    &lt;conversionRule conversionWord=&quot;clr&quot; converterClass=&quot;org.springframework.boot.logging.logback.ColorConverter&quot;/&gt;    &lt;conversionRule conversionWord=&quot;wex&quot; converterClass=&quot;org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter&quot;/&gt;    &lt;conversionRule conversionWord=&quot;wEx&quot; converterClass=&quot;org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter&quot;/&gt;    &lt;!--输出到控制台--&gt;    &lt;appender name=&quot;CONSOLE&quot; class=&quot;ch.qos.logback.core.ConsoleAppender&quot;&gt;        &lt;encoder&gt;            &lt;!-- 彩色日志格式 --&gt;            &lt;Pattern&gt;                %clr(${service}){magenta} %clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){yellow} %clr(%5p) %clr(${PID:- }){magenta} %clr([%15.15t]){faint} %clr(---){red} %clr(%-80.80logger{79}){cyan} %clr(:){blue} %m%n            &lt;/Pattern&gt;            &lt;!-- 设置字符集 --&gt;            &lt;charset&gt;UTF-8&lt;/charset&gt;        &lt;/encoder&gt;    &lt;/appender&gt;    &lt;!--输出到文件--&gt;    &lt;!-- 时间滚动输出 level为 DEBUG 日志 --&gt;    &lt;appender name=&quot;DEBUG_FILE&quot; class=&quot;ch.qos.logback.core.rolling.RollingFileAppender&quot;&gt;        &lt;!-- 正在记录的日志文件的路径及文件名 --&gt;        &lt;file&gt;${logDir}/log_debug.log&lt;/file&gt;        &lt;!--日志文件输出格式--&gt;        &lt;encoder&gt;            &lt;pattern&gt;%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n&lt;/pattern&gt;            &lt;charset&gt;UTF-8&lt;/charset&gt; &lt;!-- 设置字符集 --&gt;        &lt;/encoder&gt;        &lt;!-- 日志记录器的滚动策略，按日期，按大小记录 --&gt;        &lt;rollingPolicy class=&quot;ch.qos.logback.core.rolling.TimeBasedRollingPolicy&quot;&gt;            &lt;!-- 日志归档 --&gt;            &lt;fileNamePattern&gt;${logDir}/debug/log-debug-%d{yyyy-MM-dd}.%i.log&lt;/fileNamePattern&gt;            &lt;timeBasedFileNamingAndTriggeringPolicy class=&quot;ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP&quot;&gt;                &lt;maxFileSize&gt;100MB&lt;/maxFileSize&gt;            &lt;/timeBasedFileNamingAndTriggeringPolicy&gt;            &lt;!--日志文件保留天数--&gt;            &lt;maxHistory&gt;15&lt;/maxHistory&gt;        &lt;/rollingPolicy&gt;        &lt;!-- 此日志文件只记录debug级别的 --&gt;        &lt;filter class=&quot;ch.qos.logback.classic.filter.LevelFilter&quot;&gt;            &lt;level&gt;debug&lt;/level&gt;            &lt;onMatch&gt;ACCEPT&lt;/onMatch&gt;            &lt;onMismatch&gt;DENY&lt;/onMismatch&gt;        &lt;/filter&gt;    &lt;/appender&gt;    &lt;!-- 时间滚动输出 level为 INFO 日志 --&gt;    &lt;appender name=&quot;INFO_FILE&quot; class=&quot;ch.qos.logback.core.rolling.RollingFileAppender&quot;&gt;        &lt;!-- 正在记录的日志文件的路径及文件名 --&gt;        &lt;file&gt;${logDir}/log_info.log&lt;/file&gt;        &lt;!--日志文件输出格式--&gt;        &lt;encoder&gt;            &lt;pattern&gt;%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n&lt;/pattern&gt;            &lt;charset&gt;UTF-8&lt;/charset&gt;        &lt;/encoder&gt;        &lt;!-- 日志记录器的滚动策略，按日期，按大小记录 --&gt;        &lt;rollingPolicy class=&quot;ch.qos.logback.core.rolling.TimeBasedRollingPolicy&quot;&gt;            &lt;!-- 每天日志归档路径以及格式 --&gt;            &lt;fileNamePattern&gt;${logDir}/info/log-info-%d{yyyy-MM-dd}.%i.log&lt;/fileNamePattern&gt;            &lt;timeBasedFileNamingAndTriggeringPolicy class=&quot;ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP&quot;&gt;                &lt;maxFileSize&gt;100MB&lt;/maxFileSize&gt;            &lt;/timeBasedFileNamingAndTriggeringPolicy&gt;            &lt;!--日志文件保留天数--&gt;            &lt;maxHistory&gt;15&lt;/maxHistory&gt;        &lt;/rollingPolicy&gt;        &lt;!-- 此日志文件只记录info级别的 --&gt;        &lt;filter class=&quot;ch.qos.logback.classic.filter.LevelFilter&quot;&gt;            &lt;level&gt;info&lt;/level&gt;            &lt;onMatch&gt;ACCEPT&lt;/onMatch&gt;            &lt;onMismatch&gt;DENY&lt;/onMismatch&gt;        &lt;/filter&gt;    &lt;/appender&gt;    &lt;!-- 时间滚动输出 level为 WARN 日志 --&gt;    &lt;appender name=&quot;WARN_FILE&quot; class=&quot;ch.qos.logback.core.rolling.RollingFileAppender&quot;&gt;        &lt;!-- 正在记录的日志文件的路径及文件名 --&gt;        &lt;file&gt;${logDir}/log_warn.log&lt;/file&gt;        &lt;!--日志文件输出格式--&gt;        &lt;encoder&gt;            &lt;pattern&gt;%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n&lt;/pattern&gt;            &lt;charset&gt;UTF-8&lt;/charset&gt; &lt;!-- 此处设置字符集 --&gt;        &lt;/encoder&gt;        &lt;!-- 日志记录器的滚动策略，按日期，按大小记录 --&gt;        &lt;rollingPolicy class=&quot;ch.qos.logback.core.rolling.TimeBasedRollingPolicy&quot;&gt;            &lt;fileNamePattern&gt;${logDir}/warn/log-warn-%d{yyyy-MM-dd}.%i.log&lt;/fileNamePattern&gt;            &lt;timeBasedFileNamingAndTriggeringPolicy class=&quot;ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP&quot;&gt;                &lt;maxFileSize&gt;100MB&lt;/maxFileSize&gt;            &lt;/timeBasedFileNamingAndTriggeringPolicy&gt;            &lt;!--日志文件保留天数--&gt;            &lt;maxHistory&gt;15&lt;/maxHistory&gt;        &lt;/rollingPolicy&gt;        &lt;!-- 此日志文件只记录warn级别的 --&gt;        &lt;filter class=&quot;ch.qos.logback.classic.filter.LevelFilter&quot;&gt;            &lt;level&gt;warn&lt;/level&gt;            &lt;onMatch&gt;ACCEPT&lt;/onMatch&gt;            &lt;onMismatch&gt;DENY&lt;/onMismatch&gt;        &lt;/filter&gt;    &lt;/appender&gt;    &lt;!-- 时间滚动输出 level为 ERROR 日志 --&gt;    &lt;appender name=&quot;ERROR_FILE&quot; class=&quot;ch.qos.logback.core.rolling.RollingFileAppender&quot;&gt;        &lt;!-- 正在记录的日志文件的路径及文件名 --&gt;        &lt;file&gt;${logDir}/log_error.log&lt;/file&gt;        &lt;!--日志文件输出格式--&gt;        &lt;encoder&gt;            &lt;pattern&gt;%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n&lt;/pattern&gt;            &lt;charset&gt;UTF-8&lt;/charset&gt; &lt;!-- 此处设置字符集 --&gt;        &lt;/encoder&gt;        &lt;!-- 日志记录器的滚动策略，按日期，按大小记录 --&gt;        &lt;rollingPolicy class=&quot;ch.qos.logback.core.rolling.TimeBasedRollingPolicy&quot;&gt;            &lt;fileNamePattern&gt;${logDir}/error/log-error-%d{yyyy-MM-dd}.%i.log&lt;/fileNamePattern&gt;            &lt;timeBasedFileNamingAndTriggeringPolicy class=&quot;ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP&quot;&gt;                &lt;maxFileSize&gt;100MB&lt;/maxFileSize&gt;            &lt;/timeBasedFileNamingAndTriggeringPolicy&gt;            &lt;!--日志文件保留天数--&gt;            &lt;maxHistory&gt;15&lt;/maxHistory&gt;        &lt;/rollingPolicy&gt;        &lt;!-- 此日志文件只记录ERROR级别的 --&gt;        &lt;filter class=&quot;ch.qos.logback.classic.filter.LevelFilter&quot;&gt;            &lt;level&gt;ERROR&lt;/level&gt;            &lt;onMatch&gt;ACCEPT&lt;/onMatch&gt;            &lt;onMismatch&gt;DENY&lt;/onMismatch&gt;        &lt;/filter&gt;    &lt;/appender&gt;    &lt;!--        &lt;logger&gt;用来设置某一个包或者具体的某一个类的日志打印级别、        以及指定&lt;appender&gt;。&lt;logger&gt;仅有一个name属性，        一个可选的level和一个可选的addtivity属性。        name:用来指定受此logger约束的某一个包或者具体的某一个类。        level:用来设置打印级别，大小写无关：TRACE, DEBUG, INFO, WARN, ERROR, ALL 和 OFF，              还有一个特俗值INHERITED或者同义词NULL，代表强制执行上级的级别。              如果未设置此属性，那么当前logger将会继承上级的级别。        addtivity:是否向上级logger传递打印信息。默认是true。    --&gt;    &lt;!--&lt;logger name=&quot;org.springframework.web&quot; level=&quot;info&quot;/&gt;--&gt;    &lt;!--&lt;logger name=&quot;org.springframework.scheduling.annotation.ScheduledAnnotationBeanPostProcessor&quot; level=&quot;INFO&quot;/&gt;--&gt;    &lt;!--        使用mybatis的时候，sql语句是debug下才会打印，而这里我们只配置了info，所以想要查看sql语句的话，有以下两种操作：        第一种把&lt;root level=&quot;info&quot;&gt;改成&lt;root level=&quot;DEBUG&quot;&gt;这样就会打印sql，不过这样日志那边会出现很多其他消息        第二种就是单独给dao下目录配置debug模式，代码如下，这样配置sql语句会打印，其他还是正常info级别：     --&gt;    &lt;!--        root节点是必选节点，用来指定最基础的日志输出级别，只有一个level属性        level:用来设置打印级别，大小写无关：TRACE, DEBUG, INFO, WARN, ERROR, ALL 和 OFF，        不能设置为INHERITED或者同义词NULL。默认是DEBUG        可以包含零个或多个元素，标识这个appender将会添加到这个logger。    --&gt;    &lt;!-- 开发环境:打印控制台 就不输出到文件中 --&gt;    &lt;springProfile name=&quot;dev&quot;&gt;        &lt;logger name=&quot;com.ehootu.service.core.mapper&quot; level=&quot;debug&quot; additivity=&quot;false&quot;&gt;            &lt;appender-ref ref=&quot;CONSOLE&quot;/&gt;        &lt;/logger&gt;        &lt;root level=&quot;info&quot;&gt;            &lt;appender-ref ref=&quot;CONSOLE&quot;/&gt;        &lt;/root&gt;    &lt;/springProfile&gt;    &lt;!-- 生产环境:输出到文件 就不打印到控制台了 --&gt;    &lt;springProfile name=&quot;prod&quot;&gt;        &lt;logger name=&quot;com.ehootu.service.core.mapper&quot; level=&quot;debug&quot; additivity=&quot;false&quot;&gt;            &lt;appender-ref ref=&quot;DEBUG_FILE&quot;/&gt;        &lt;/logger&gt;        &lt;root level=&quot;info&quot;&gt;            &lt;appender-ref ref=&quot;DEBUG_FILE&quot;/&gt;            &lt;appender-ref ref=&quot;INFO_FILE&quot;/&gt;            &lt;appender-ref ref=&quot;WARN_FILE&quot;/&gt;            &lt;appender-ref ref=&quot;ERROR_FILE&quot;/&gt;        &lt;/root&gt;    &lt;/springProfile&gt;&lt;/configuration&gt;</code></pre><p>这样一个多环境日志配置就完成了</p><blockquote><p>在你发怒的时候，要紧闭你的嘴，免得增加你的怒气。——苏格拉底</p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
      <tag>spring boot</tag>
      
      <tag>logback</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>spring的@Transactional注解详细用法</title>
    <link href="undefined2019/11/06/spring%E7%9A%84@Transactional%E6%B3%A8%E8%A7%A3%E8%AF%A6%E7%BB%86%E7%94%A8%E6%B3%95/"/>
    <url>2019/11/06/spring%E7%9A%84@Transactional%E6%B3%A8%E8%A7%A3%E8%AF%A6%E7%BB%86%E7%94%A8%E6%B3%95/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1561431519943.jpg" srcset="undefined" alt=""></p><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>事务管理对于企业应用来说是至关重要的，即使出现异常情况，它也可以保证数据的一致性。<br>Spring Framework对事务管理提供了一致的抽象，其特点如下：</p><ul><li>为不同的事务API提供一致的编程模型，比如JTA(Java Transaction API), JDBC,Hibernate, JPA(Java Persistence API和JDO(Java Data Objects)</li><li>支持声明式事务管理，特别是基于注解的声明式事务管理，简单易用</li><li>提供比其他事务API如JTA更简单的编程式事务管理API</li><li>与spring数据访问抽象的完美集成</li></ul><hr><h2 id="事务管理方式"><a href="#事务管理方式" class="headerlink" title="事务管理方式"></a>事务管理方式</h2><p>spring支持编程式事务管理和声明式事务管理两种方式。<br>编程式事务管理使用TransactionTemplate或者直接使用底层的PlatformTransactionManager。对于编程式事务管理，spring推荐使用TransactionTemplate。</p><p>声明式事务管理建立在AOP之上的。其本质是对方法前后进行拦截，然后在目标方法开始之前创建或者加入一个事务，在执行完目标方法之后根据执行情况提交或者回滚事务。声明式事务最大的优点就是不需要通过编程的方式管理事务，这样就不需要在业务逻辑代码中掺杂事务管理的代码，只需在配置文件中做相关的事务规则声明(或通过基于<code>@Transactional</code>注解的方式)，便可以将事务规则应用到业务逻辑中。</p><p>显然声明式事务管理要优于编程式事务管理，这正是spring倡导的非侵入式的开发方式。声明式事务管理使业务代码不受污染，一个普通的POJO对象，只要加上注解就可以获得完全的事务支持。和编程式事务相比，声明式事务唯一不足地方是，后者的最细粒度只能作用到方法级别，无法做到像编程式事务那样可以作用到代码块级别。但是即便有这样的需求，也存在很多变通的方法，比如，可以将需要进行事务管理的代码块独立为方法等等。<br>声明式事务管理也有两种常用的方式，一种是基于tx和aop名字空间的xml配置文件，另一种就是基于<code>@Transactional</code>注解。显然基于注解的方式更简单易用，更清爽。</p><h3 id="springmvc配置注解"><a href="#springmvc配置注解" class="headerlink" title="springmvc配置注解"></a>springmvc配置注解</h3><pre><code class="xml">&lt;!-- 事务：第一步，配置事务管理器 --&gt;&lt;bean id=&quot;transactionManager&quot; class=&quot;org.springframework.jdbc.datasource.DataSourceTransactionManager&quot;&gt;        &lt;!--注入属性dataSource,表明事务管理器要对哪个数据源进行管理  --&gt;        &lt;property name=&quot;dataSource&quot; ref=&quot;dataSource&quot;/&gt;    &lt;/bean&gt;&lt;!--事务注解方式： 第二步，开启事务注解  【在需要事务管理的方法上加上@Transactional注解即可】--&gt;&lt;tx:annotation-driven transaction-manager=&quot;transactionManager&quot; proxy-target-class=&quot;true&quot;/&gt;</code></pre><h3 id="spring-boot开启事务"><a href="#spring-boot开启事务" class="headerlink" title="spring boot开启事务"></a>spring boot开启事务</h3><p>在程序入口处添加 <code>@EnableTransactionManagement</code> 注解</p><hr><h2 id="Spring事务的传播行为"><a href="#Spring事务的传播行为" class="headerlink" title="Spring事务的传播行为"></a>Spring事务的传播行为</h2><h5 id="1-事务隔离级别"><a href="#1-事务隔离级别" class="headerlink" title="1.事务隔离级别"></a>1.事务隔离级别</h5><p>隔离级别是指若干个并发的事务之间的隔离程度。TransactionDefinition 接口中定义了五个表示隔离级别的常量：</p><ul><li><p><code>TransactionDefinition.ISOLATION_DEFAULT</code>：这是默认值，表示使用底层数据库的默认隔离级别。对大部分数据库而言，通常这值就是<code>TransactionDefinition.ISOLATION_READ_COMMITTED</code>。</p></li><li><p><code>TransactionDefinition.ISOLATION_READ_UNCOMMITTED</code>：该隔离级别表示一个事务可以读取另一个事务修改但还没有提交的数据。该级别不能防止脏读，不可重复读和幻读，因此很少使用该隔离级别。比如PostgreSQL实际上并没有此级别。</p></li><li><p>TransactionDefinition.ISOLATION_READ_COMMITTED：该隔离级别表示一个事务只能读取另一个事务已经提交的数据。该级别可以防止脏读，这也是大多数情况下的推荐值。</p></li><li><p><code>TransactionDefinition.ISOLATION_REPEATABLE_READ</code>：该隔离级别表示一个事务在整个过程中可以多次重复执行某个查询，并且每次返回的记录都相同。该级别可以防止脏读和不可重复读。</p></li><li><p><code>TransactionDefinition.ISOLATION_SERIALIZABLE</code>：所有的事务依次逐个执行，这样事务之间就完全不可能产生干扰，也就是说，该级别可以防止脏读、不可重复读以及幻读。但是这将严重影响程序的性能。通常情况下也不会用到该级别。</p></li></ul><h5 id="2-事务传播行为"><a href="#2-事务传播行为" class="headerlink" title="2.事务传播行为"></a>2.事务传播行为</h5><p>所谓事务的传播行为是指，如果在开始当前事务之前，一个事务上下文已经存在，此时有若干选项可以指定一个事务性方法的执行行为。在<code>TransactionDefinition</code>定义中包括了如下几个表示传播行为的常量：</p><ul><li><code>TransactionDefinition.PROPAGATION_REQUIRED</code>：如果当前存在事务，则加入该事务；如果当前没有事务，则创建一个新的事务。这是默认值。</li></ul><ul><li><code>TransactionDefinition.PROPAGATION_REQUIRES_NEW</code>：创建一个新的事务，如果当前存在事务，则把当前事务挂起。</li></ul><ul><li><code>TransactionDefinition.PROPAGATION_SUPPORTS</code>：如果当前存在事务，则加入该事务；如果当前没有事务，则以非事务的方式继续运行。</li></ul><ul><li><code>TransactionDefinition.PROPAGATION_NOT_SUPPORTED</code>：以非事务方式运行，如果当前存在事务，则把当前事务挂起。</li></ul><ul><li><code>TransactionDefinition.PROPAGATION_NEVER</code>：以非事务方式运行，如果当前存在事务，则抛出异常。</li></ul><ul><li><code>TransactionDefinition.PROPAGATION_MANDATORY</code>：如果当前存在事务，则加入该事务；如果当前没有事务，则抛出异常。</li></ul><ul><li><code>TransactionDefinition.PROPAGATION_NESTED</code>：如果当前存在事务，则创建一个事务作为当前事务的嵌套事务来运行；如果当前没有事务，则该取值等价于TransactionDefinition.PROPAGATION_REQUIRED。</li></ul><h5 id="3-事务超时"><a href="#3-事务超时" class="headerlink" title="3.事务超时"></a>3.事务超时</h5><p>所谓事务超时，就是指一个事务所允许执行的最长时间，如果超过该时间限制但事务还没有完成，则自动回滚事务。在<code>TransactionDefinition</code>中以 int 的值来表示超时时间，其单位是秒。<br>默认设置为底层事务系统的超时值，如果底层数据库事务系统没有设置超时值，那么就是none，没有超时限制。</p><hr><h2 id="spring事务回滚规则"><a href="#spring事务回滚规则" class="headerlink" title="spring事务回滚规则"></a>spring事务回滚规则</h2><p>Spring的AOP即声明式事务管理默认是针对unchecked exception回滚。Spring的事务边界是在调用业务方法之前开始的，业务方法执行完毕之后来执行commit or rollback(Spring默认取决于是否抛出runtimeException)。<br>例如：<code>@Transactional(rollbackFor = Exception.class)</code> 在事务注解上添加<code>rollbackFor</code>就可以针对<code>checked</code>异常进行事务回滚了</p><hr><h2 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h2><p><code>@Transactional</code> 可以作用于接口、接口方法、类以及类方法上。当作用于类上时，该类的所有 public 方法将都具有该类型的事务属性，同时，我们也可以在方法级别使用该标注来覆盖类级别的定义。</p><p>虽然<code>@Transactional</code>注解可以作用于接口、接口方法、类以及类方法上，但是 Spring 建议不要在接口或者接口方法上使用该注解，因为这只有在使用基于接口的代理时它才会生效。另外，<code>@Transactional</code>注解应该只被应用到 public 方法上，这是由 Spring AOP 的本质决定的。如果你在 protected、private 或者默认可见性的方法上使用<code>@Transactional</code>注解，这将被忽略，也不会抛出任何异常。</p><p>默认情况下，只有来自外部的方法调用才会被AOP代理捕获，也就是，类内部方法调用本类内部的其他方法并不会引起事务行为，即使被调用方法使用<code>@Transactional</code>注解进行修饰。</p><blockquote><p>参考：<a href="https://juejin.im/post/5d0f3788f265da1bb67a22c5" target="_blank" rel="noopener">https://juejin.im/post/5d0f3788f265da1bb67a22c5</a> <a href="https://blog.csdn.net/u013142781/article/details/50421904" target="_blank" rel="noopener">https://blog.csdn.net/u013142781/article/details/50421904</a></p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring boot</tag>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>记一次Spring Boot整合shiro、jwt实现前后端分离</title>
    <link href="undefined2019/11/06/%E8%AE%B0%E4%B8%80%E6%AC%A1spring%20boot%E6%95%B4%E5%90%88shiro%E3%80%81jwt%E5%AE%9E%E7%8E%B0%E5%89%8D%E5%90%8E%E7%AB%AF%E5%88%86%E7%A6%BB/"/>
    <url>2019/11/06/%E8%AE%B0%E4%B8%80%E6%AC%A1spring%20boot%E6%95%B4%E5%90%88shiro%E3%80%81jwt%E5%AE%9E%E7%8E%B0%E5%89%8D%E5%90%8E%E7%AB%AF%E5%88%86%E7%A6%BB/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1561433183670.jpg" srcset="undefined" alt=""></p><p>前后端分离无非就是前端只是写前端，后台专注接口，交互就使用token进行交互。</p><p><a href="https://gitee.com/LUO_yuan" target="_blank" rel="noopener" title="源码地址">源码地址</a><br>欢迎star</p><h2 id="1-实现AuthenticationToken接口，重写token"><a href="#1-实现AuthenticationToken接口，重写token" class="headerlink" title="1 实现AuthenticationToken接口，重写token"></a>1 实现AuthenticationToken接口，重写token</h2><p><code>AuthToken</code>:</p><pre><code class="java">package com.luoyuanxiangvip.admin.core.shiro;import org.apache.shiro.authc.AuthenticationToken;/** * &lt;p&gt; * 自定义token * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/26 18:49 */public class AuthToken implements AuthenticationToken {    private static final long serialVersionUID = 2424386775104576139L;    /** 自定义 token */    private String token;    public AuthToken(String token) {        this.token = token;    }    @Override    public Object getPrincipal() {        return token;    }    @Override    public Object getCredentials() {        return token;    }}</code></pre><h2 id="2-创建生成token工具"><a href="#2-创建生成token工具" class="headerlink" title="2 创建生成token工具"></a>2 创建生成token工具</h2><p>导入jwt工具包:</p><pre><code class="xml">&lt;dependency&gt;   &lt;groupId&gt;io.jsonwebtoken&lt;/groupId&gt;   &lt;artifactId&gt;jjwt&lt;/artifactId&gt;   &lt;version&gt;0.9.0&lt;/version&gt;&lt;/dependency&gt;</code></pre><p>创建<code>JwtProperties</code>、<code>JwtToken</code>类用于生成token<br><code>JwtProperties</code></p><pre><code class="java">package com.luoyuanxiangvip.admin.core.jwt;import lombok.Getter;import lombok.Setter;import org.springframework.boot.context.properties.ConfigurationProperties;import org.springframework.stereotype.Component;/** * &lt;p&gt; * jwt配置 * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/26 18:53 */@Getter@Setter@Component@ConfigurationProperties(&quot;jwt&quot;)public class JwtProperties {    private String header;    private String secret;    private Long expiration;    private String md5Key;}</code></pre><p><code>JwtToken</code></p><pre><code class="java">package com.luoyuanxiangvip.admin.core.jwt;import io.jsonwebtoken.Claims;import io.jsonwebtoken.Jwts;import io.jsonwebtoken.SignatureAlgorithm;import org.springframework.stereotype.Component;import javax.annotation.Resource;import java.util.Date;import java.util.HashMap;import java.util.Map;/** * &lt;p&gt; * 自定义token * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/26 18:53 */@Componentpublic class JwtToken {    @Resource    private JwtProperties jwtProperties;    /**     * 获取用户名从token中     * @param token token     * @return 主题或用户名     */    public String getUsernameFromToken(String token) {        try {            return getClaimFromToken(token).getSubject();        } catch (Exception e) {            return null;        }    }    /**     * 获取jwt发布时间     * @param token token     * @return 时间     */    public Date getIssuedAtDateFromToken(String token) {        try {            return getClaimFromToken(token).getIssuedAt();        } catch (Exception e) {            return null;        }    }    /**     * 获取jwt失效时间     * @param token token     * @return 时间     */    public Date getExpirationDateFromToken(String token){        try {            return getClaimFromToken(token).getExpiration();        } catch (Exception e) {            return null;        }    }    /**     * 获取jwt接收者     * @param token token     * @return string     */    public String getAudienceFromToken(String token){        try {            return getClaimFromToken(token).getAudience();        } catch (Exception e) {            return null;        }    }    /**     * 获取私有的jwt claim     * @param token token     * @param key key     * @return String     */    private String getPrivateClaimFromToken(String token, String key){        try {            return getClaimFromToken(token).get(key).toString();        } catch (Exception e) {            return null;        }    }    /**     * 获取md5 key从token中     * @param token token     * @return string     */    public String getMd5KeyFromToken(String token){        return getPrivateClaimFromToken(token, jwtProperties.getMd5Key());    }    /**     * 获取jwt的payload部分     * @param token token     * @return Claims     */    private Claims getClaimFromToken(String token) throws Exception {        return Jwts.parser()                .setSigningKey(jwtProperties.getSecret())                .parseClaimsJws(token)                .getBody();    }    /**     * 解析token是否正确,不正确会报异常     * @param token token     */    public boolean parseToken(String token){        Claims body = null;        try {            body = getClaimFromToken(token);        } catch (Exception e) {            return true;        }        return body.size() &lt;= 0;    }    /**     * 验证token是否失效     * true:过期   false:没过期     * @param token token     * @return Boolean     */    public Boolean isTokenExpired(String token){        final Date expiration = getExpirationDateFromToken(token);        return expiration.before(new Date());    }    /**     * 生成token(通过用户名和签名时候用的随机数)     * @param userName 用户名     * @param randomKey 用户密码或key     * @return token     */    public String generateToken(String userName, String randomKey) {        Map&lt;String, Object&gt; claims = new HashMap&lt;&gt;(1);        claims.put(jwtProperties.getMd5Key(), randomKey);        return doGenerateToken(claims, userName);    }    /**     * token 刷新     * @param token token     * @return new token     */    public String refreshToken(String token) throws Exception {        final Date createdDate = new Date();        final Date expirationDate = new Date(createdDate.getTime() + jwtProperties.getExpiration() * 1000);        String subject = this.getUsernameFromToken(token);        Claims claims = this.getClaimFromToken(token);        return Jwts.builder()                .setClaims(claims)                .setSubject(subject)                .setIssuedAt(createdDate)                .setExpiration(expirationDate)                .signWith(SignatureAlgorithm.HS512, jwtProperties.getSecret())                .compact();    }    /**     * 生成token     * @param claims 需要加密的用户信息     * @param subject 主题     * @return token     */    private String doGenerateToken(Map&lt;String, Object&gt; claims, String subject) {        final Date createdDate = new Date();        final Date expirationDate = new Date(createdDate.getTime() + jwtProperties.getExpiration() * 1000);        return Jwts.builder()                .setClaims(claims)                .setSubject(subject)                .setIssuedAt(createdDate)                .setExpiration(expirationDate)                .signWith(SignatureAlgorithm.HS512, jwtProperties.getSecret())                .compact();    }}</code></pre><p><code>application-dev.yml</code></p><pre><code class="yaml">jwt:  #请求头  header: Authorization  secret: defaultSecret  # token 过期时间 单位秒 7天  expiration: 604800  # md5key  md5Key: randomKey</code></pre><h2 id="3-创建拦截器对token进行拦截处理"><a href="#3-创建拦截器对token进行拦截处理" class="headerlink" title="3 创建拦截器对token进行拦截处理"></a>3 创建拦截器对token进行拦截处理</h2><p><code>JwtFilter</code> 判断是否存在token</p><pre><code class="java">package com.luoyuanxiangvip.admin.core.filter;import com.luoyuanxiangvip.admin.core.shiro.AuthToken;import org.apache.shiro.web.filter.authc.BasicHttpAuthenticationFilter;import javax.servlet.ServletRequest;import javax.servlet.ServletResponse;/** * &lt;p&gt; * jwtfilter * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/27 9:50 */public class JwtFilter extends BasicHttpAuthenticationFilter {    /**     * 判断用户是否想要登入。     * 检测header里面是否包含Authorization字段即可     */    @Override    protected boolean isLoginAttempt(ServletRequest request, ServletResponse response) {        String authorization = getAuthzHeader(request);        return authorization != null;    }    /**     * 创建自定义token     */    @Override    protected boolean executeLogin(ServletRequest request, ServletResponse response) throws Exception {        String authorization = getAuthzHeader(request);        AuthToken token = new AuthToken(authorization);        // 提交给realm进行登入，如果错误他会抛出异常并被捕获        getSubject(request, response).login(token);        // 如果没有抛出异常则代表登入成功，返回true        return true;    }    /**     * 这里我们详细说明下为什么最终返回的都是true，即允许访问     * 例如我们提供一个地址 GET /article     * 登入用户和游客看到的内容是不同的     * 如果在这里返回了false，请求会被直接拦截，用户看不到任何东西     * 所以我们在这里返回true，Controller中可以通过 subject.isAuthenticated() 来判断用户是否登入     * 如果有些资源只有登入用户才能访问，我们只需要在方法上面加上 @RequiresAuthentication 注解即可     * 但是这样做有一个缺点，就是不能够对GET,POST等请求进行分别过滤鉴权(因为我们重写了官方的方法)，但实际上对应用影响不大     * 每一个方法都需要添加 @RequiresAuthentication 是因为我们重写了 token 全局异常捕获不到 所以需要 @RequiresAuthentication 来进行验证捕获异常     */    @Override    protected boolean isAccessAllowed(ServletRequest request, ServletResponse response, Object mappedValue) {        if (isLoginAttempt(request, response)) {            try {                executeLogin(request, response);            } catch (Exception ignored) {}        }        return true;    }}</code></pre><p><code>AccessFilter</code></p><pre><code class="java">package com.luoyuanxiangvip.admin.core.filter;import com.alibaba.fastjson.JSONObject;import com.luoyuanxiangvip.core.utils.AjaxResult;import org.apache.commons.lang3.StringUtils;import org.apache.shiro.web.filter.AccessControlFilter;import javax.servlet.ServletRequest;import javax.servlet.ServletResponse;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;/** * &lt;p&gt; * 是否携带token访问 * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/27 9:49 */public class AccessFilter extends AccessControlFilter {    private static final String AUTHORIZATION_HEADER = &quot;Authorization&quot;;    /**     * 可以发现他是调用的isAccessAllowed方法和onAccessDenied方法，只要两者有一个可以就可以了，     * 从名字中我们也可以理解，他的逻辑是这样：     * 先调用isAccessAllowed，如果返回的是true，     * 则直接放行执行后面的filter和servlet，如果返回的是false，     * 则继续执行后面的onAccessDenied方法，     * 如果后面返回的是true则也可以有权限继续执行后面的filter和servelt。     * 只有两个函数都返回false才会阻止后面的filter和servlet的执行。     * @param request request     * @param response response     * @param mappedValue mappedValue     * @return boolean     * @throws Exception Exception     */    @Override    protected boolean isAccessAllowed(ServletRequest request, ServletResponse response, Object mappedValue) throws Exception {        HttpServletRequest req = (HttpServletRequest) request;        String authorization = req.getHeader(AUTHORIZATION_HEADER);        // 如果没有token 那么就不能访问资源 返回false 后面的调用方法不执行        return StringUtils.isNotEmpty(authorization);    }    @Override    protected boolean onAccessDenied(ServletRequest request, ServletResponse response) throws Exception {        HttpServletRequest req = (HttpServletRequest) request;        HttpServletResponse res = (HttpServletResponse) response;        res.setHeader(&quot;Access-Control-Allow-Origin&quot;, req.getHeader(&quot;Origin&quot;));        res.setHeader(&quot;Access-Control-Allow-Credentials&quot;,&quot;true&quot;);        res.setHeader(&quot;Access-Control-Allow-Methods&quot;, &quot;GET,POST,PUT,DELETE,PATCH,OPTIONS&quot;);        res.setHeader(&quot;Access-Control-Allow-Headers&quot;, &quot;Content-Type, Accept, Authorization&quot;);        res.setCharacterEncoding(&quot;UTF-8&quot;);        res.getWriter().println(JSONObject.toJSONString(AjaxResult.error(50008,&quot;token无效&quot;)));        return false;    }}</code></pre><h2 id="4-创建ShiroConfig"><a href="#4-创建ShiroConfig" class="headerlink" title="4 创建ShiroConfig"></a>4 创建ShiroConfig</h2><pre><code class="java">package com.luoyuanxiangvip.admin.core.config;import com.luoyuanxiangvip.admin.core.filter.AccessFilter;import com.luoyuanxiangvip.admin.core.filter.JwtFilter;import com.luoyuanxiangvip.admin.core.shiro.UserRealm;import org.apache.shiro.mgt.DefaultSessionStorageEvaluator;import org.apache.shiro.mgt.DefaultSubjectDAO;import org.apache.shiro.spring.LifecycleBeanPostProcessor;import org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor;import org.apache.shiro.spring.web.ShiroFilterFactoryBean;import org.apache.shiro.web.mgt.DefaultWebSecurityManager;import org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator;import org.springframework.context.annotation.Bean;import org.springframework.context.annotation.Configuration;import org.springframework.context.annotation.DependsOn;import javax.servlet.Filter;import java.util.HashMap;import java.util.Map;/** * &lt;p&gt; * shiro配置 * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/26 18:47 */@Configurationpublic class ShiroConfig {    @Bean(&quot;securityManager&quot;)    public DefaultWebSecurityManager securityManager() {        DefaultWebSecurityManager manager = new DefaultWebSecurityManager();        // 使用自己的realm        manager.setRealm(userRealm());        /*         * 关闭shiro自带的session，详情见文档         * http://shiro.apache.org/session-management.html#SessionManagement-StatelessApplications%28Sessionless%29         */        DefaultSubjectDAO subjectDAO = new DefaultSubjectDAO();        DefaultSessionStorageEvaluator defaultSessionStorageEvaluator = new DefaultSessionStorageEvaluator();        defaultSessionStorageEvaluator.setSessionStorageEnabled(false);        subjectDAO.setSessionStorageEvaluator(defaultSessionStorageEvaluator);        manager.setSubjectDAO(subjectDAO);        return manager;    }    @Bean    public UserRealm userRealm() {        return new UserRealm();    }    @Bean(&quot;shiroFilter&quot;)    public ShiroFilterFactoryBean factory(DefaultWebSecurityManager securityManager) {        ShiroFilterFactoryBean factoryBean = new ShiroFilterFactoryBean();        // 添加自己的过滤器并且取名为jwt        Map&lt;String, Filter&gt; filterMap = new HashMap&lt;&gt;();        filterMap.put(&quot;jwt&quot;, new JwtFilter());        filterMap.put(&quot;accessFilter&quot;,new AccessFilter());        factoryBean.setFilters(filterMap);        factoryBean.setSecurityManager(securityManager);        /*         * 自定义url规则         * http://shiro.apache.org/web.html#urls-         */        Map&lt;String, String&gt; filterRuleMap = new HashMap&lt;&gt;();        filterRuleMap.put(&quot;/login&quot;,&quot;anon&quot;);        filterRuleMap.put(&quot;/logout&quot;,&quot;anon&quot;);        filterRuleMap.put(&quot;/swagger-ui.html&quot;,&quot;anon&quot;);        filterRuleMap.put(&quot;/v2/**&quot;,&quot;anon&quot;);        filterRuleMap.put(&quot;/webjars/**&quot;, &quot;anon&quot;);        filterRuleMap.put(&quot;/swagger-resources/**&quot;, &quot;anon&quot;);        filterRuleMap.put(&quot;/swagger/**&quot;,&quot;anon&quot;);        filterRuleMap.put(&quot;/druid/**&quot;,&quot;anon&quot;);        filterRuleMap.put(&quot;/editor.md/**&quot;,&quot;anon&quot;);        filterRuleMap.put(&quot;/admin/article/update1&quot;, &quot;anon&quot;);        // 所有请求通过我们自己的JWT Filter        filterRuleMap.put(&quot;/**&quot;, &quot;jwt,accessFilter&quot;);        factoryBean.setFilterChainDefinitionMap(filterRuleMap);        return factoryBean;    }    /**     * 下面的代码是添加注解支持     */    @Bean    @DependsOn(&quot;lifecycleBeanPostProcessor&quot;)    public DefaultAdvisorAutoProxyCreator defaultAdvisorAutoProxyCreator() {        DefaultAdvisorAutoProxyCreator defaultAdvisorAutoProxyCreator = new DefaultAdvisorAutoProxyCreator();        // 强制使用cglib，防止重复代理和可能引起代理出错的问题        // https://zhuanlan.zhihu.com/p/29161098        defaultAdvisorAutoProxyCreator.setProxyTargetClass(true);        return defaultAdvisorAutoProxyCreator;    }    @Bean    public LifecycleBeanPostProcessor lifecycleBeanPostProcessor() {        return new LifecycleBeanPostProcessor();    }    @Bean    public AuthorizationAttributeSourceAdvisor authorizationAttributeSourceAdvisor(DefaultWebSecurityManager securityManager) {        AuthorizationAttributeSourceAdvisor advisor = new AuthorizationAttributeSourceAdvisor();        advisor.setSecurityManager(securityManager);        return advisor;    }}</code></pre><h2 id="5-创建认证类"><a href="#5-创建认证类" class="headerlink" title="5 创建认证类"></a>5 创建认证类</h2><p><code>UserRealm</code></p><pre><code class="java">package com.luoyuanxiangvip.admin.core.shiro;import com.luoyuanxiangvip.admin.core.exception.ExpiredException;import com.luoyuanxiangvip.admin.core.exception.InvalidException;import com.luoyuanxiangvip.admin.core.exception.PasswordException;import com.luoyuanxiangvip.admin.core.jwt.JwtToken;import com.luoyuanxiangvip.core.constant.Constant;import com.luoyuanxiangvip.core.entity.SysUser;import com.luoyuanxiangvip.core.service.ISysUserService;import org.apache.shiro.authc.AuthenticationException;import org.apache.shiro.authc.AuthenticationInfo;import org.apache.shiro.authc.AuthenticationToken;import org.apache.shiro.authc.SimpleAuthenticationInfo;import org.apache.shiro.authz.AuthorizationInfo;import org.apache.shiro.authz.SimpleAuthorizationInfo;import org.apache.shiro.realm.AuthorizingRealm;import org.apache.shiro.subject.PrincipalCollection;import javax.annotation.Resource;import javax.servlet.http.HttpSession;/** * &lt;p&gt; * 用户认证 * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/26 18:49 */public class UserRealm extends AuthorizingRealm {    @Resource    private JwtToken jwtToken;    @Resource    private ISysUserService iSysUserService;    @Resource    private HttpSession session;    /**     * 大坑！，必须重写此方法，不然Shiro会报错     */    @Override    public boolean supports(AuthenticationToken token) {        return token instanceof AuthToken;    }    /**     * 权限认证     *     * @param principals pr     * @return auth     */    @Override    protected AuthorizationInfo doGetAuthorizationInfo(PrincipalCollection principals) {        return new SimpleAuthorizationInfo();    }    /**     * 登陆认证     *     * @param authenticationToken token     * @return auth     * @throws AuthenticationException auth     */    @Override    protected AuthenticationInfo doGetAuthenticationInfo(AuthenticationToken authenticationToken) throws AuthenticationException {        String token = (String) authenticationToken.getCredentials();        if (jwtToken.parseToken(token)) {            throw new AuthenticationException(new InvalidException(&quot;token无效！&quot;));        }        String userName = jwtToken.getUsernameFromToken(token);        SysUser byUserName = iSysUserService.findByUserName(userName);        if (byUserName == null) {            throw new AuthenticationException(&quot;用户不存在！&quot;);        }        if (!byUserName.getPassword().equals(jwtToken.getMd5KeyFromToken(token))) {            throw new AuthenticationException(new PasswordException(&quot;密码错误!&quot;));        }        if (jwtToken.isTokenExpired(token)) {            throw new AuthenticationException(new ExpiredException(&quot;token过期!&quot;));        }        session.setAttribute(Constant.USER, byUserName);        return new SimpleAuthenticationInfo(token, token, getName());    }}</code></pre><h2 id="6-提供登陆接口"><a href="#6-提供登陆接口" class="headerlink" title="6 提供登陆接口"></a>6 提供登陆接口</h2><pre><code class="java">@ApiOperation(value = &quot;用户登陆&quot;)    @Logs    @PostMapping(&quot;/login&quot;)    public AjaxResult login(@Valid @RequestBody LoginVo loginVo) throws AuthenticationException {        String encryptionPassword = Sha256Util.sha256(loginVo.getPassword(), loginVo.getUsername());        String accessToken = jwtToken.generateToken(loginVo.getUsername(), encryptionPassword);        AuthToken authToken = new AuthToken(accessToken);        Subject subject = SecurityUtils.getSubject();        subject.login(authToken);        if (subject.isAuthenticated()) {            JwtVo jwtVo = new JwtVo();            long accessExpiration = jwtToken.getExpirationDateFromToken(accessToken).getTime();            long createTime = jwtToken.getIssuedAtDateFromToken(accessToken).getTime();            jwtVo.setAccessToken(accessToken);            jwtVo.setAccessExpiration(accessExpiration);            jwtVo.setCreateTime(createTime);            return AjaxResult.success(jwtVo);        }        return AjaxResult.error(&quot;登陆失败&quot;);    }</code></pre><p><code>LoginVo</code></p><pre><code class="java">package com.luoyuanxiangvip.admin.core.vo;import io.swagger.annotations.ApiModelProperty;import lombok.Data;import javax.validation.constraints.NotBlank;import java.io.Serializable;/** * &lt;p&gt; * * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/27 10:19 */@Datapublic class LoginVo implements Serializable {    private static final long serialVersionUID = 7158840114330656683L;    @ApiModelProperty(value = &quot;登陆名&quot;)    @NotBlank(message = &quot;账号不能为空&quot;)    private String username;    @ApiModelProperty(value = &quot;密码&quot;)    @NotBlank(message = &quot;密码不能为空&quot;)    private String password;}</code></pre><p><code>JwtVo</code></p><pre><code class="java">package com.luoyuanxiangvip.admin.core.vo;import io.swagger.annotations.ApiModelProperty;import lombok.Data;import java.io.Serializable;/** * &lt;p&gt; * * &lt;/p&gt; * * @author luoyuanxiang &lt;p&gt;luoyuanxiangvip.com&lt;/p&gt; * @since 2019/5/27 10:19 */@Datapublic class JwtVo implements Serializable {    private static final long serialVersionUID = -1299095840932717137L;    @ApiModelProperty(value = &quot;请求token&quot;)    private String accessToken;    @ApiModelProperty(value = &quot;请求token的过期时间 单位：秒&quot;)    private long accessExpiration;    @ApiModelProperty(value = &quot;创建时间 单位：秒&quot;)    private long createTime;}</code></pre><p>到此springboot前后端分离整合完毕，有不合理的地方望大神提出，谢谢</p><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring boot</tag>
      
      <tag>shiro</tag>
      
      <tag>jwt</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>代码性能优化总结</title>
    <link href="undefined2019/11/06/35%20%E4%B8%AA%20Java%20%E4%BB%A3%E7%A0%81%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E6%80%BB%E7%BB%93/"/>
    <url>2019/11/06/35%20%E4%B8%AA%20Java%20%E4%BB%A3%E7%A0%81%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E6%80%BB%E7%BB%93/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1562842253566.jpg" srcset="undefined" alt=""></p><h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>代码优化，一个很重要的课题。可能有些人觉得没用，一些细小的地方有什么好修改的，改与不改对于代码的运行效率有什么影响呢？这个问题我是这么考虑的，就像大海里面的鲸鱼一样，它吃一条小虾米有用吗？没用，但是，吃的小虾米一多之后，鲸鱼就被喂饱了。代码优化也是一样，如果项目着眼于尽快无BUG上线，那么此时可以抓大放小，代码的细节可以不精打细磨；但是如果有足够的时间开发、维护代码，这时候就必须考虑每个可以优化的细节了，一个一个细小的优化点累积起来，对于代码的运行效率绝对是有提升的。</p><p>代码优化的目标是：</p><p>1、减小代码的体积</p><p>2、提高代码运行的效率</p><h3 id="代码优化细节"><a href="#代码优化细节" class="headerlink" title="代码优化细节"></a>代码优化细节</h3><p><strong>1、尽量指定类、方法的final修饰符</strong></p><p>带有final修饰符的类是不可派生的。在<code>Java</code>核心API中，有许多应用<code>final</code>的例子，例如<code>java.lang.String</code>，整个类都是<code>final</code>的。为类指定<code>final</code>修饰符可以让类不可以被继承，为方法指定<code>final</code>修饰符可以让方法不可以被重写。如果指定了一个类为<code>final</code>，则该类所有的方法都是<code>final</code>的。<code>Java</code>编译器会寻找机会内联所有的<code>final</code>方法，内联对于提升<code>Java</code>运行效率作用重大，具体参见<code>Java</code>运行期优化。此举能够使性能平均提高50%。</p><p><strong>2、尽量重用对象</strong></p><p>特别是<code>String</code>对象的使用，出现字符串连接时应该使用<code>StringBuilder/StringBuffer</code>代替。由于<code>Java</code>虚拟机不仅要花时间生成对象，以后可能还需要花时间对这些对象进行垃圾回收和处理，因此，生成过多的对象将会给程序的性能带来很大的影响。</p><p><strong>3、尽可能使用局部变量</strong></p><p>调用方法时传递的参数以及在调用中创建的临时变量都保存在栈中速度较快，其他变量，如静态变量、实例变量等，都在堆中创建，速度较慢。另外，栈中创建的变量，随着方法的运行结束，这些内容就没了，不需要额外的垃圾回收。</p><p><strong>4、及时关闭流</strong></p><p>Java编程过程中，进行数据库连接、I/O流操作时务必小心，在使用完毕后，及时关闭以释放资源。因为对这些大对象的操作会造成系统大的开销，稍有不慎，将会导致严重的后果。</p><p><strong>5、尽量减少对变量的重复计算</strong></p><p>明确一个概念，对方法的调用，即使方法中只有一句语句，也是有消耗的，包括创建栈帧、调用方法时保护现场、调用方法完毕时恢复现场等。所以例如下面的操作：</p><pre><code class="java">for (int i = 0; i &lt; list.size(); i++){...}</code></pre><p>建议替换为：</p><pre><code class="java">for (int i = 0, int length = list.size(); i &lt; length; i++){...}</code></pre><p>这样，在<code>list.size()</code>很大的时候，就减少了很多的消耗</p><p><strong>6、尽量采用懒加载的策略，即在需要的时候才创建</strong></p><p>例如：</p><pre><code class="java">String str = &quot;aaa&quot;;if (i == 1){list.add(str);}</code></pre><p>建议替换为：</p><pre><code class="java">if (i == 1){String str = &quot;aaa&quot;;list.add(str);}</code></pre><p><strong>7、慎用异常</strong></p><p>异常对性能不利。抛出异常首先要创建一个新的对象，<code>Throwable</code>接口的构造函数调用名为<code>fillInStackTrace(</code>)的本地同步方法，<code>fillInStackTrace()</code>方法检查堆栈，收集调用跟踪信息。只要有异常被抛出，Java虚拟机就必须调整调用堆栈，因为在处理过程中创建了一个新的对象。异常只能用于错误处理，不应该用来控制程序流程。</p><p><strong>8、不要在循环中使用<code>try…catch…</code>，应该把其放在最外层</strong></p><p>除非不得已。如果毫无理由地这么写了，只要你的领导资深一点、有强迫症一点，八成就要骂你为什么写出这种垃圾代码来了</p><p><strong>9、如果能估计到待添加的内容长度，为底层以数组方式实现的集合、工具类指定初始长度</strong></p><p>比如<code>ArrayList、LinkedLlist、StringBuilder、StringBuffer、HashMap、HashSet</code>等等，以<code>StringBuilder</code>为例：</p><p>（1）<code>StringBuilder()</code>　　　　　　// 默认分配16个字符的空间</p><p>（2）<code>StringBuilder(int size)</code>　　// 默认分配size个字符的空间</p><p>（3）<code>StringBuilder(String str)</code>　// 默认分配16个字符+str.length()个字符空间</p><p>可以通过类（这里指的不仅仅是上面的<code>StringBuilder</code>）的来设定它的初始化容量，这样可以明显地提升性能。比如<code>StringBuilder</code>吧，<code>length</code>表示当前的<code>StringBuilder</code>能保持的字符数量。因为当<code>StringBuilder</code>达到最大容量的时候，它会将自身容量增加到当前的2倍再加2，无论何时只要<code>StringBuilder</code>达到它的最大容量，它就不得不创建一个新的字符数组然后将旧的字符数组内容拷贝到新字符数组中—-这是十分耗费性能的一个操作。试想，如果能预估到字符数组中大概要存放5000个字符而不指定长度，最接近5000的2次幂是4096，每次扩容加的2不管，那么：</p><p>（1）在4096 的基础上，再申请8194个大小的字符数组，加起来相当于一次申请了12290个大小的字符数组，如果一开始能指定5000个大小的字符数组，就节省了一倍以上的空间</p><p>（2）把原来的4096个字符拷贝到新的的字符数组中去</p><p>这样，既浪费内存空间又降低代码运行效率。所以，给底层以数组实现的集合、工具类设置一个合理的初始化容量是错不了的，这会带来立竿见影的效果。但是，注意，像HashMap这种是以数组+链表实现的集合，别把初始大小和你估计的大小设置得一样，因为一个<code>table</code>上只连接一个对象的可能性几乎为0。初始大小建议设置为2的N次幂，如果能估计到有2000个元素，设置成<code>new HashMap(128)、new HashMap(256)</code>都可以。</p><p><strong>10、当复制大量数据时，使用<code>System.arraycopy()</code>命令</strong></p><p><strong>11、乘法和除法使用移位操作</strong></p><p>例如：</p><pre><code class="java">for (val = 0; val &lt; 100000; val += 5){a = val * 8;b = val / 2;}</code></pre><p>用移位操作可以极大地提高性能，因为在计算机底层，对位的操作是最方便、最快的，因此建议修改为：</p><pre><code class="java">for (val = 0; val &lt; 100000; val += 5){a = val &lt;&lt; 3;b = val &gt;&gt; 1;}</code></pre><p>移位操作虽然快，但是可能会使代码不太好理解，因此最好加上相应的注释。</p><p><strong>12、循环内不要不断创建对象引用</strong></p><p>例如：</p><pre><code class="java">for (int i = 1; i &lt;= count; i++){Object obj = new Object();}</code></pre><p>这种做法会导致内存中有count份Object对象引用存在，count很大的话，就耗费内存了，建议为改为：</p><pre><code class="java">Object obj = null;for (int i = 0; i &lt;= count; i++) { obj = new Object(); }</code></pre><p><strong>13、基于效率和类型检查的考虑，应该尽可能使用<code>array</code>，无法确定数组大小时才使用<code>ArrayList</code></strong></p><p><strong>14、尽量使用<code>HashMap、ArrayList、StringBuilder</code>，除非线程安全需要，否则不推荐使用<code>Hashtable、Vector、StringBuffer</code>，后三者由于使用同步机制而导致了性能开销</strong></p><p><strong>15、不要将数组声明为<code>public static final</code></strong></p><p>因为这毫无意义，这样只是定义了引用为<code>static final</code>，数组的内容还是可以随意改变的，将数组声明<code>为public</code>更是一个安全漏洞，这意味着这个数组可以被外部类所改变</p><p><strong>16、尽量在合适的场合使用单例</strong></p><p>使用单例可以减轻加载的负担、缩短加载的时间、提高加载的效率，但并不是所有地方都适用于单例，简单来说，单例主要适用于以下三个方面：</p><p>（1）控制资源的使用，通过线程同步来控制资源的并发访问</p><p>（2）控制实例的产生，以达到节约资源的目的</p><p>（3）控制数据的共享，在不建立直接关联的条件下，让多个不相关的进程或线程之间实现通信</p><p><strong>17、尽量避免随意使用静态变量</strong></p><p>要知道，当某个对象被定义为<code>static</code>的变量所引用，那么gc通常是不会回收这个对象所占有的堆内存的，如：</p><pre><code class="java">public class A{ private static B b = new B();}</code></pre><p>此时静态变量b的生命周期与A类相同，如果A类不被卸载，那么引用B指向的B对象会常驻内存，直到程序终止</p><p><strong>18、及时清除不再需要的会话</strong></p><p>为了清除不再活动的会话，许多应用服务器都有默认的会话超时时间，一般为30分钟。当应用服务器需要保存更多的会话时，如果内存不足，那么操作系统会把部分数据转移到磁盘，应用服务器也可能根据MRU（最近最频繁使用）算法把部分不活跃的会话转储到磁盘，甚至可能抛出内存不足的异常。如果会话要被转储到磁盘，那么必须要先被序列化，在大规模集群中，对对象进行序列化的代价是很昂贵的。因此，当会话不再需要时，应当及时调用<code>HttpSession</code>的<code>invalidate()</code>方法清除会话。</p><p><strong>19、实现RandomAccess接口的集合比如<code>ArrayList</code>，应当使用最普通的<code>for</code>循环而不是<code>foreach</code>循环来遍历</strong></p><p>这是JDK推荐给用户的。JDK API对于<code>RandomAccess</code>接口的解释是：实现<code>RandomAccess</code>接口用来表明其支持快速随机访问，此接口的主要目的是允许一般的算法更改其行为，从而将其应用到随机或连续访问列表时能提供良好的性能。实际经验表明，实现<code>RandomAccess</code>接口的类实例，假如是随机访问的，使用普通<code>for</code>循环效率将高于使用<code>foreach</code>循环；反过来，如果是顺序访问的，则使用<code>Iterator</code>会效率更高。可以使用类似如下的代码作判断：</p><pre><code class="java">if (list instanceof RandomAccess){ for (int i = 0; i &lt; list.size(); i++){}}else{Iterator&lt;?&gt; iterator = list.iterable(); while (iterator.hasNext()){iterator.next()}}</code></pre><p><code>foreach</code>循环的底层实现原理就是迭代器<code>Iterator</code>，参见Java语法糖1：可变长度参数以及<code>foreach</code>循环原理。所以后半句”反过来，如果是顺序访问的，则使用<code>Iterator</code>会效率更高”的意思就是顺序访问的那些类实例，使用<code>foreach</code>循环去遍历。</p><p><strong>20、使用同步代码块替代同步方法</strong></p><p>这点在多线程模块中的<code>synchronized</code>锁方法块一文中已经讲得很清楚了，除非能确定一整个方法都是需要进行同步的，否则尽量使用同步代码块，避免对那些不需要进行同步的代码也进行了同步，影响了代码执行效率。</p><p><strong>21、将常量声明为<code>static final</code>，并以大写命名</strong></p><p>这样在编译期间就可以把这些内容放入常量池中，避免运行期间计算生成常量的值。另外，将常量的名字以大写命名也可以方便区分出常量与变量</p><p><strong>22、不要创建一些不使用的对象，不要导入一些不使用的类</strong></p><p>这毫无意义，如果代码中出现”The value of the local variable i is not used”、”The import java.util is never used”，那么请删除这些无用的内容</p><p><strong>23、程序运行过程中避免使用反射</strong></p><p>关于，请参见反射。反射是Java提供给用户一个很强大的功能，功能强大往往意味着效率不高。不建议在程序运行过程中使用尤其是频繁使用反射机制，特别是<code>Method</code>的<code>invoke</code>方法，如果确实有必要，一种建议性的做法是将那些需要通过反射加载的类在项目启动的时候通过反射实例化出一个对象并放入内存—-用户只关心和对端交互的时候获取最快的响应速度，并不关心对端的项目启动花多久时间。</p><p><strong>24、使用数据库连接池和线程池</strong></p><p>这两个池都是用于重用对象的，前者可以避免频繁地打开和关闭连接，后者可以避免频繁地创建和销毁线程</p><p><strong>25、使用带缓冲的输入输出流进行IO操作</strong></p><p>带缓冲的输入输出流，即<code>BufferedReader、BufferedWriter、BufferedInputStream、BufferedOutputStream</code>，这可以极大地提升IO效率</p><p><strong>26、顺序插入和随机访问比较多的场景使用<code>ArrayList</code>，元素删除和中间插入比较多的场景使用<code>LinkedList</code></strong></p><blockquote><p>参考：<a href="https://www.cnblogs.com/wwwcnblogscom/p/8036411.html" target="_blank" rel="noopener">https://www.cnblogs.com/wwwcnblogscom/p/8036411.html</a></p></blockquote><p><strong>27、不要让<code>public</code>方法中有太多的形参</strong></p><p><code>public</code>方法即对外提供的方法，如果给这些方法太多形参的话主要有两点坏处：</p><p>1、违反了面向对象的编程思想，Java讲求一切都是对象，太多的形参，和面向对象的编程思想并不契合</p><p>2、参数太多势必导致方法调用的出错概率增加</p><p>至于这个”太多”指的是多少个，3、4个吧。比如我们用JDBC写一个<code>insertStudentInfo</code>方法，有10个学生信息字段要插如<code>Student</code>表中，可以把这10个参数封装在一个实体类中，作为<code>insert</code>方法的形参</p><p><strong>28、字符串变量和字符串常量<code>equals</code>的时候将字符串常量写在前面</strong></p><p>这是一个比较常见的小技巧了，如果有以下代码：</p><pre><code class="java">String str = &quot;123&quot;;if (str.equals(&quot;123&quot;)) {...}</code></pre><p>建议修改为：</p><pre><code class="java">String str = &quot;123&quot;;if (&quot;123&quot;.equals(str)){...}</code></pre><p>这么做主要是可以避免空指针异常</p><p><strong>29、请知道，在java中if (i == 1)和if (1 == i)是没有区别的，但从阅读习惯上讲，建议使用前者</strong></p><p>平时有人问，”if (i == 1)”和”if (1== i)”有没有区别，这就要从C/C++讲起。</p><p>在C/C++中，”if (i == 1)”判断条件成立，是以0与非0为基准的，0表示false，非0表示true，如果有这么一段代码：</p><pre><code class="java">int i = 2;if (i == 1){...}else{...}</code></pre><p>C/C++判断”i==1″不成立，所以以0表示，即false。但是如果：</p><pre><code class="java">int i = 2;if (i = 1) { ... }else{ ... }</code></pre><p>万一程序员一个不小心，把”if (i == 1)”写成”if (i = 1)”，这样就有问题了。在if之内将i赋值为1，if判断里面的内容非0，返回的就是true了，但是明明i为2，比较的值是1，应该返回的false。这种情况在C/C++的开发中是很可能发生的并且会导致一些难以理解的错误产生，所以，为了避免开发者在if语句中不正确的赋值操作，建议将if语句写为：</p><pre><code class="java">int i = 2;if (1 == i) { ... }else{ ... }</code></pre><p>这样，即使开发者不小心写成了”1 = i”，C/C++编译器也可以第一时间检查出来，因为我们可以对一个变量赋值i为1，但是不能对一个常量赋值1为i。</p><p>但是，在Java中，C/C++这种”if (i = 1)”的语法是不可能出现的，因为一旦写了这种语法，Java就会编译报错”Type mismatch: cannot convert from int to boolean”。但是，尽管Java的”if (i == 1)”和”if (1 == i)”在语义上没有任何区别，但是从阅读习惯上讲，建议使用前者会更好些。</p><p><strong>30、不要对数组使用<code>toString()</code>方法</strong></p><p>看一下对数组使用<code>toString()</code>打印出来的是什么：</p><pre><code class="java">public static void main(String[] args){ int[] is = new int[]{1, 2, 3};System.out.println(is.toString());}</code></pre><p>结果是：<br><code>[I@18a992f</code><br>本意是想打印出数组内容，却有可能因为数组引用is为空而导致空指针异常。不过虽然对数组<code>toString()</code>没有意义，但是对集合<code>toString()</code>是可以打印出集合里面的内容的，因为集合的父类<code>AbstractCollections&lt;E&gt;</code>重写了<code>Object</code>的<code>toString()</code>方法。</p><p><strong>31、不要对超出范围的基本数据类型做向下强制转型</strong></p><p>这绝不会得到想要的结果：</p><pre><code class="java">public static void main(String[] args){ long l = 12345678901234L;int i = (int)l;System.out.println(i);}</code></pre><p>我们可能期望得到其中的某几位，但是结果却是：</p><p>1942892530</p><p>解释一下。Java中long是8个字节64位的，所以12345678901234在计算机中的表示应该是：</p><p>0000 0000 0000 0000 0000 1011 0011 1010 0111 0011 1100 1110 0010 1111 1111 0010</p><p>一个int型数据是4个字节32位的，从低位取出上面这串二进制数据的前32位是：</p><p>0111 0011 1100 1110 0010 1111 1111 0010</p><p>这串二进制表示为十进制1942892530，所以就是我们上面的控制台上输出的内容。从这个例子上还能顺便得到两个结论：</p><p>1、整型默认的数据类型是<code>int，long l = 12345678901234L</code>，这个数字已经超出了<code>int</code>的范围了，所以最后有一个L，表示这是一个<code>long</code>型数。顺便，浮点型的默认类型是<code>double</code>，所以定义<code>float</code>的时候要写成<code>float f = 3.5f</code></p><p>2、接下来再写一句<code>int ii = l + i;</code>会报错，因为<code>long + int</code>是一个<code>long</code>，不能赋值给<code>int</code></p><p><strong>32、公用的集合类中不使用的数据一定要及时<code>remove</code>掉</strong></p><p>如果一个集合类是公用的（也就是说不是方法里面的属性），那么这个集合里面的元素是不会自动释放的，因为始终有引用指向它们。所以，如果公用集合里面的某些数据不使用而不去<code>remove</code>掉它们，那么将会造成这个公用集合不断增大，使得系统有内存泄露的隐患。</p><p><strong>33、把一个基本数据类型转为字符串，基本数据类型.toString()是最快的方式、String.valueOf(数据)次之、数据+””最慢</strong></p><p>把一个基本数据类型转为一般有三种方式，我有一个Integer型数据i，可以使用<code>i.toString()、String.valueOf(i)、i+&quot;&quot;</code>三种方式，三种方式的效率如何，看一个测试：</p><pre><code class="java">public static void main(String[] args){ int loopTime = 50000;Integer i = 0; long startTime = System.currentTimeMillis(); for (int j = 0; j &lt; loopTime; j++){String str = String.valueOf(i);}System.out.println(&quot;String.valueOf()：&quot; + (System.currentTimeMillis() - startTime) + &quot;ms&quot;);startTime = System.currentTimeMillis(); for (int j = 0; j &lt; loopTime; j++){String str = i.toString();}System.out.println(&quot;Integer.toString()：&quot; + (System.currentTimeMillis() - startTime) + &quot;ms&quot;);startTime = System.currentTimeMillis(); for (int j = 0; j &lt; loopTime; j++){String str = i + &quot;&quot;;}System.out.println(&quot;i + \&quot;\&quot;：&quot; + (System.currentTimeMillis() - startTime) + &quot;ms&quot;);}</code></pre><p>运行结果为：<br><code>String.valueOf()：11ms Integer.toString()：5ms i + &quot;&quot;：25ms</code><br>所以以后遇到把一个基本数据类型转为<code>String</code>的时候，优先考虑使用<code>toString()</code>方法。至于为什么，很简单：</p><p>1、<code>String.valueOf()</code>方法底层调用了<code>Integer.toString()</code>方法，但是会在调用前做空判断</p><p>2、<code>Integer.toString()</code>方法就不说了，直接调用了</p><p>3、i + “”底层使用了<code>StringBuilder</code>实现，先用append方法拼接，再用<code>toString()</code>方法获取字符串</p><p>三者对比下来，明显是2最快、1次之、3最慢</p><p><strong>34、使用最有效率的方式去遍历Map</strong></p><p>遍历Map的方式有很多，通常场景下我们需要的是遍历Map中的Key和Value，那么推荐使用的、效率最高的方式是：</p><pre><code class="java">public static void main(String[] args){HashMap&lt;String, String&gt; hm = new HashMap&lt;String, String&gt;();hm.put(&quot;111&quot;, &quot;222&quot;);Set&lt;Map.Entry&lt;String, String&gt;&gt; entrySet = hm.entrySet();Iterator&lt;Map.Entry&lt;String, String&gt;&gt; iter = entrySet.iterator(); while (iter.hasNext()){Map.Entry&lt;String, String&gt; entry = iter.next();System.out.println(entry.getKey() + &quot;\t&quot; + entry.getValue());}}</code></pre><p>如果你只是想遍历一下这个<code>Map</code>的<code>key</code>值，那用<code>Set&lt;String&gt; keySet = hm.keySet();</code>会比较合适一些</p><p><strong>35、对资源的close()建议分开操作</strong></p><p>意思是，比如我有这么一段代码：</p><pre><code class="java">try{XXX.close();YYY.close();}catch (Exception e){...}</code></pre><p>建议修改为：</p><pre><code class="java">try{ XXX.close(); }catch (Exception e) { ... }try{ YYY.close(); }catch (Exception e) { ... }</code></pre><p>虽然有些麻烦，却能避免资源泄露。我们想，如果没有修改过的代码，万一<code>XXX.close()</code>抛异常了，那么就进入了<code>cath</code>块中了，<code>YYY.close()</code>不会执行，YYY这块资源就不会回收了，一直占用着，这样的代码一多，是可能引起资源句柄泄露的。而改为下面的写法之后，就保证了无论如何XXX和YYY都会被close掉。</p><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis五种数据结构简介</title>
    <link href="undefined2019/11/06/Redis%E4%BA%94%E7%A7%8D%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E7%AE%80%E4%BB%8B/"/>
    <url>2019/11/06/Redis%E4%BA%94%E7%A7%8D%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E7%AE%80%E4%BB%8B/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1562842253566.jpg" srcset="undefined" alt="封面"></p><h3 id="Redis五种结构"><a href="#Redis五种结构" class="headerlink" title="Redis五种结构"></a>Redis五种结构</h3><h4 id="1、String"><a href="#1、String" class="headerlink" title="1、String"></a>1、String</h4><p>可以是字符串，整数或者浮点数，对整个字符串或者字符串中的一部分执行操作，对整个整数或者浮点执行自增(<code>increment</code>)或者自减(<code>decrement</code>)操作。</p><p>字符串命令：</p><p>①get、获取存储在指定键中的值</p><p>②set、设置存储在指定键中的值</p><p>③del、删除存储在指定键中的值（这个命令可以用于所有的类型）</p><h4 id="2-list"><a href="#2-list" class="headerlink" title="2.list"></a>2.list</h4><p>一个链表，链表上的每个节点都包含了一个字符串，虫链表的两端推入或者弹出元素，根据偏移量对链表进行修剪(trim)，读取单个或者多个元素，根据值查找或者移除元素。</p><p>列表命令：</p><p>①rpush、将给定值推入列表的右端</p><p>②lrange、获取列表在指定范围上的所有值</p><p>③lindex、获取列表在指定范围上的单个元素</p><p>④lpop、从列表的左端弹出一个值，并返回被弹出的值</p><h4 id="3-set"><a href="#3-set" class="headerlink" title="3.set"></a>3.set</h4><p>包含字符串的无序收集器(unordered collection)、并且被包含的每个字符串都是独一无二的。添加，获取，移除单个元素，检查一个元素是否存在于集合中，计算交集，并集，差集，从集合里面随机获取元素。</p><p>集合命令：</p><p>①sadd、将给定元素添加到集合</p><p>②smembers、返回集合包含的所有元素</p><p>③sismember、检查指定元素是否存在于集合中</p><p>④srem、检查指定元素是否存在于集合中，那么移除这个元素</p><h4 id="4-hash"><a href="#4-hash" class="headerlink" title="4.hash"></a>4.hash</h4><p>包含键值对无序散列表，添加，获取，移除当键值对，获取所有键值对。</p><p>散列命令：</p><p>①hset、在散列里面关联起指定的键值对</p><p>②hget、获取指定散列键的值</p><p>③hgetall、获取散列包含的所有键值对</p><p>④hdel、如果给定键存在于散列里面，那么移除这个键</p><h4 id="5-zset"><a href="#5-zset" class="headerlink" title="5.zset"></a>5.zset</h4><p>字符串成员(member)与浮点数分值(score)之间的有序映射，元素的排列顺序由分值的大小决定。添加，获取，删除单个元素，根据分值范围(range)或者成员来获取元素。</p><p>有序集合命令：</p><p>①zadd、将一个带有给定分值的成员添加到有序集合里面</p><p>②zrange、根据元素在有序排列中所处的位置，从有序集合里面获取多个元素</p><p>③zrangebyscore、获取有序集合在给定分值范围内的所有元素</p><p>④zrem、如果指定成员存在于有序集合中，那么移除这个成员</p><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Spring事务的基本原理、传播属性、隔离级别</title>
    <link href="undefined2019/11/06/Spring%E4%BA%8B%E5%8A%A1%E7%9A%84%E5%9F%BA%E6%9C%AC%E5%8E%9F%E7%90%86%E3%80%81%E4%BC%A0%E6%92%AD%E5%B1%9E%E6%80%A7%E3%80%81%E9%9A%94%E7%A6%BB%E7%BA%A7%E5%88%AB/"/>
    <url>2019/11/06/Spring%E4%BA%8B%E5%8A%A1%E7%9A%84%E5%9F%BA%E6%9C%AC%E5%8E%9F%E7%90%86%E3%80%81%E4%BC%A0%E6%92%AD%E5%B1%9E%E6%80%A7%E3%80%81%E9%9A%94%E7%A6%BB%E7%BA%A7%E5%88%AB/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1562935835183.jpg" srcset="undefined" alt="事务"></p><h1 id="一、事务的基本要素（ACID）"><a href="#一、事务的基本要素（ACID）" class="headerlink" title="一、事务的基本要素（ACID）"></a>一、事务的基本要素（ACID）</h1><h3 id="事务是指多个操作单元组成的合集，多个单元操作是整体不可分割的，要么都操作不成功，要么都成功。其必须遵循四个原则（ACID）。"><a href="#事务是指多个操作单元组成的合集，多个单元操作是整体不可分割的，要么都操作不成功，要么都成功。其必须遵循四个原则（ACID）。" class="headerlink" title="事务是指多个操作单元组成的合集，多个单元操作是整体不可分割的，要么都操作不成功，要么都成功。其必须遵循四个原则（ACID）。"></a>事务是指多个操作单元组成的合集，多个单元操作是整体不可分割的，要么都操作不成功，要么都成功。其必须遵循四个原则（ACID）。</h3><ul><li>原子性（<code>Atomicity</code>）：指一个事务要么全部执行,要么不执行，也就是说一个事务不可能只执行了一半就停止了.比如你从取款机取钱,这个事务可以分成两个步骤:1划卡,2出钱.不可能划了卡,而钱却没出来.这两步必须同时完成.要么就不完成。</li><li>一致性（<code>Consistency</code>）：在事务执行前数据库的数据处于正确的状态，而事务执行完成后数据库的数据还是应该处于正确的状态，即数据完整性约束没有被破坏；如银行转帐，A转帐给B，必须保证A的钱一定转给B，一定不会出现A的钱转了但B没收到，否则数据库的数据就处于不一致（不正确）的状态。</li><li>隔离性（<code>Isolation</code>）：并发事务执行之间互不影响，在一个事务内部的操作对其他事务是不产生影响，这需要事务隔离级别来指定隔离性；</li><li>持久性（<code>Durability</code>）：事务一旦执行成功，它对数据库的数据的改变必须是永久的，不会因比如遇到系统故障或断电造成数据不一致或丢失。</li></ul><h1 id="二、事务的分类"><a href="#二、事务的分类" class="headerlink" title="二、事务的分类"></a>二、事务的分类</h1><ul><li>数据库分为本地事务跟全局事务</li></ul><ol><li>本地事务：普通事务，独立一个数据库，能保证在该数据库上操作的ACID。</li><li>分布式事务：涉及两个或多个数据库源的事务，即跨越多台同类或异类数据库的事务（由每台数据库的本地事务组成的），分布式事务旨在保证这些本地事务的所有操作的ACID，使事务可以跨越多台数据库；</li></ol><ul><li>Java事务类型分为JDBC事务跟JTA事务</li></ul><ol><li>JDBC事务：即为上面说的数据库事务中的本地事务，通过connection对象控制管理。</li><li>JTA事务：JTA指Java事务API(Java Transaction API)，是Java EE数据库事务规范， JTA只提供了事务管理接口，由应用程序服务器厂商（如WebSphere Application Server）提供实现，JTA事务比JDBC更强大，支持分布式事务。</li></ol><ul><li>按是否通过编程分为声明式事务和编程式事务，参考:<a href="http://blog.csdn.net/liaohaojian/article/details/70139151" target="_blank" rel="noopener">链接</a></li></ul><ol><li>声明式事务：通过XML配置或者注解实现。</li><li>编程式事务：通过编程代码在业务逻辑时需要时自行实现，粒度更小。</li></ol><h1 id="三、事务的基本原理"><a href="#三、事务的基本原理" class="headerlink" title="三、事务的基本原理"></a>三、事务的基本原理</h1><p>Spring事务的本质其实就是数据库对事务的支持，没有数据库的事务支持，spring是无法提供事务功能的。对于纯JDBC操作数据库，想要用到事务，可以按照以下步骤进行：</p><ol><li>获取连接 <code>Connection con = DriverManager.getConnection()</code></li><li>开启事务<code>con.setAutoCommit(true/false);</code></li><li>执行CRUD</li><li>提交事务/回滚事务 <code>con.commit() / con.rollback();</code></li><li>关闭连接 <code>conn.close();</code></li></ol><p>使用Spring的事务管理功能后，我们可以不再写步骤 2 和 4 的代码，而是由Spirng 自动完成。 那么Spring是如何在我们书写的 CRUD 之前和之后开启事务和关闭事务的呢？解决这个问题，也就可以从整体上理解Spring的事务管理实现原理了。下面简单地介绍下，注解方式为例子</p><ol><li>配置文件开启注解驱动，在相关的类和方法上通过注解<code>@Transactional</code>标识。</li><li>spring 在启动的时候会去解析生成相关的bean，这时候会查看拥有相关注解的类和方法，并且为这些类和方法生成代理，并根据<code>@Transaction</code>的相关参数进行相关配置注入，这样就在代理中为我们把相关的事务处理掉了（开启正常提交事务，异常回滚事务）。</li><li>真正的数据库层的事务提交和回滚是通过binlog或者redo log实现的。</li></ol><h1 id="四、数据库隔离级别"><a href="#四、数据库隔离级别" class="headerlink" title="四、数据库隔离级别"></a>四、数据库隔离级别</h1><table><thead><tr><th>隔离级别</th><th>隔离级别的值</th><th>脏读</th><th>不可重复读</th><th>幻读</th></tr></thead><tbody><tr><td>读未提交Read-Uncommitted</td><td>0</td><td>是</td><td>是</td><td>是</td></tr><tr><td>不可重复读（read-committed）</td><td>1</td><td>否</td><td>是</td><td>否</td></tr><tr><td>可重复读（repeatable-read）</td><td>2</td><td>否</td><td>否</td><td>是</td></tr><tr><td>串行化（serializable）</td><td>3</td><td>否</td><td>否</td><td>否</td></tr></tbody></table><p>脏读：一事务对数据进行了增删改，但未提交，另一事务可以读取到未提交的数据。如果第一个事务这时候回滚了，那么第二个事务就读到了脏数据。</p><p>不可重复读：一个事务中发生了两次读操作，第一次读操作和第二次操作之间，另外一个事务对数据进行了修改，这时候两次读取的数据是不一致的。</p><p>幻读：第一个事务对一定范围的数据进行批量修改，第二个事务在这个范围增加一条数据，这时候第一个事务就会丢失对新增数据的修改。</p><blockquote><p>不可重复读的和幻读很容易混淆，不可重复读侧重于修改，幻读侧重于新增或删除。解决不可重复读的问题只需锁住满足条件的行，解决幻读需要锁表</p></blockquote><p>隔离级别越高，越能保证数据的完整性和一致性，但是对并发性能的影响也越大。</p><p>大多数的数据库默认隔离级别为 Read Commited，比如 SqlServer、Oracle</p><p>少数数据库默认隔离级别为：Repeatable Read 比如： MySQL InnoDB</p><p>1、SQL规范所规定的标准，不同的数据库具体的实现可能会有些差异</p><p>2、mysql中默认事务隔离级别是可重复读时并不会锁住读取到的行</p><p>3、事务隔离级别为读提交时，写数据只会锁住相应的行</p><p>4、事务隔离级别为可重复读时，如果有索引（包括主键索引）的时候，以索引列为条件更新数据，会存在间隙锁间隙锁、行锁、下一键锁的问题，从而锁住一些行；如果没有索引，更新数据时会锁住整张表。</p><p>5、事务隔离级别为串行化时，读写数据都会锁住整张表</p><p>6、隔离级别越高，越能保证数据的完整性和一致性，但是对并发性能的影响也越大，鱼和熊掌不可兼得啊。对于多数应用程序，可以优先考虑把数据库系统的隔离级别设为Read Committed，它能够避免脏读取，而且具有较好的并发性能。尽管它会导致不可重复读、幻读这些并发问题，在可能出现这类问题的个别场合，可以由应用程序采用悲观锁或乐观锁来控制。</p><h1 id="五、Spring中的隔离级别"><a href="#五、Spring中的隔离级别" class="headerlink" title="五、Spring中的隔离级别"></a>五、Spring中的隔离级别</h1><table><thead><tr><th>常量</th><th>解释</th></tr></thead><tbody><tr><td>ISOLATION_DEFAULT</td><td>这是个 PlatfromTransactionManager 默认的隔离级别，使用数据库默认的事务隔离级别。另外四个与 JDBC 的隔离级别相对应。</td></tr><tr><td>ISOLATION_READ_UNCOMMITTED</td><td>这是事务最低的隔离级别，它充许另外一个事务可以看到这个事务未提交的数据。这种隔离级别会产生脏读，不可重复读和幻像读。</td></tr><tr><td>ISOLATION_READ_COMMITTED</td><td>保证一个事务修改的数据提交后才能被另外一个事务读取。另外一个事务不能读取该事务未提交的数据。</td></tr><tr><td>ISOLATION_REPEATABLE_READ</td><td>这种事务隔离级别可以防止脏读，不可重复读。但是可能出现幻像读。</td></tr><tr><td>ISOLATION_SERIALIZABLE</td><td>这是花费最高代价但是最可靠的事务隔离级别。事务被处理为顺序执行。</td></tr></tbody></table><ol><li><code>ISOLATION_DEFAULT</code>：用底层数据库的默认隔离级别，数据库管理员设置什么就是什么</li><li><code>ISOLATION_READ_UNCOMMITTED</code>（未提交读）：最低隔离级别、事务未提交前，就可被其他事务读取（会出现幻读、脏读、不可重复读）</li><li><code>ISOLATION_READ_COMMITTED</code>（提交读）：一个事务提交后才能被其他事务读取到（该隔离级别禁止其他事务读取到未提交事务的数据、所以还是会造成幻读、不可重复读）、sql server默认级别</li><li><code>ISOLATION_REPEATABLE_READ</code>（可重复读）：可重复读，保证多次读取同一个数据时，其值都和事务开始时候的内容是一致，禁止读取到别的事务未提交的数据（该隔离基本可防止脏读，不可重复读（重点在修改），但会出现幻读（重点在增加与删除））（MySql默认级别，更改可通过<code>set transaction isolation level</code> 级别）</li><li><code>ISOLATION_SERIALIZABLE</code>（序列化）：代价最高最可靠的隔离级别（该隔离级别能防止脏读、不可重复读、幻读）</li></ol><ul><li>丢失更新：两个事务同时更新一行数据，最后一个事务的更新会覆盖掉第一个事务的更新，从而导致第一个事务更新的数据丢失，这是由于没有加锁造成的；</li><li>幻读：同样的事务操作过程中，不同时间段多次（不同事务）读取同一数据，读取到的内容不一致（一般是行数变多或变少）。</li><li>脏读：一个事务读取到另外一个未提及事务的内容，即为脏读。</li><li>不可重复读：同一事务中，多次读取内容不一致（一般行数不变，而内容变了）。</li></ul><p>幻读与不可重复读的区别：幻读的重点在于插入与删除，即第二次查询会发现比第一次查询数据变少或者变多了，以至于给人一种幻象一样，而不可重复读重点在于修改，即第二次查询会发现查询结果比第一次查询结果不一致，即第一次结果已经不可重现了。</p><p>数据库隔离级别越高，执行代价越高，并发执行能力越差，因此在实际项目开发使用时要综合考虑，为了考虑并发性能一般使用提交读隔离级别，它能避免丢失更新和脏读，尽管不可重复读和幻读不能避免，但可以在可能出现的场合使用悲观锁或乐观锁来解决这些问题。</p><p>悲观锁与乐观锁可参考：<a href="https://blog.csdn.net/liaohaojian/article/details/62416972" target="_blank" rel="noopener">乐观锁与悲观锁随笔</a></p><h2 id="事务的并发问题"><a href="#事务的并发问题" class="headerlink" title="事务的并发问题"></a>事务的并发问题</h2><p>1、脏读：事务A读取了事务B更新的数据，然后B回滚操作，那么A读取到的数据是脏数据</p><p>2、不可重复读：事务 A 多次读取同一数据，事务 B 在事务A多次读取的过程中，对数据作了更新并提交，导致事务A多次读取同一数据时，结果 不一致。</p><p>3、幻读：系统管理员A将数据库中所有学生的成绩从具体分数改为ABCDE等级，但是系统管理员B就在这个时候插入了一条具体分数的记录，当系统管理员A改结束后发现还有一条记录没有改过来，就好像发生了幻觉一样，这就叫幻读。</p><p>小结：不可重复读的和幻读很容易混淆，不可重复读侧重于修改，幻读侧重于新增或删除。解决不可重复读的问题只需锁住满足条件的行，解决幻读需要锁表</p><h1 id="六、事务的传播属性"><a href="#六、事务的传播属性" class="headerlink" title="六、事务的传播属性"></a>六、事务的传播属性</h1><p>所谓<code>spring</code>事务的传播属性，就是定义在存在多个事务同时存在的时候，<code>spring</code>应该如何处理这些事务的行为。这些属性在 <code>TransactionDefinition</code>中定义，具体常量的解释见下表：</p><table><thead><tr><th>常量名称</th><th>常量解释</th></tr></thead><tbody><tr><td>PROPAGATION_REQUIRED</td><td>支持当前事务，如果当前没有事务，就新建一个事务。这是最常见的选择，也是 Spring 默认的事务的传播。</td></tr><tr><td>PROPAGATION_REQUIRES_NEW</td><td>新建事务，如果当前存在事务，把当前事务挂起。新建的事务将和被挂起的事务没有任何关系，是两个独立的事务，外层事务失败回滚之后，不能回滚内层事务执行的结果，内层事务失败抛出异常，外层事务捕获，也可以不处理回滚操作</td></tr><tr><td>PROPAGATION_SUPPORTS</td><td>支持当前事务，如果当前没有事务，就以非事务方式执行。</td></tr><tr><td>PROPAGATION_MANDATORY</td><td>支持当前事务，如果当前没有事务，就抛出异常。</td></tr><tr><td>PROPAGATION_NOT_SUPPORTED</td><td>以非事务方式执行操作，如果当前存在事务，就把当前事务挂起。</td></tr><tr><td>PROPAGATION_NEVER</td><td>以非事务方式执行，如果当前存在事务，则抛出异常。</td></tr><tr><td>PROPAGATION_NESTED</td><td>如果一个活动的事务存在，则运行在一个嵌套的事务中。如果没有活动事务，则按REQUIRED属性执行。它使用了一个单独的事务，这个事务拥有多个可以回滚的保存点。内部事务的回滚不会对外部事务造成影响。它只对DataSourceTransactionManager事务管理器起效。</td></tr></tbody></table><p>通过上面的理论知识的铺垫，我们大致知道了数据库事务和spring事务的一些属性和特点，接下来我们通过分析一些嵌套事务的场景，来深入理解spring事务传播的机制。</p><p>假设外层事务 <code>Service A</code> 的 <code>Method A()</code> 调用 内层<code>Service B</code> 的 <code>Method B()</code></p><h3 id="PROPAGATION-REQUIRED-spring-默认"><a href="#PROPAGATION-REQUIRED-spring-默认" class="headerlink" title="PROPAGATION_REQUIRED(spring 默认)"></a><code>PROPAGATION_REQUIRED</code>(spring 默认)</h3><p>如果<code>ServiceB.methodB()</code>的事务级别定义为 <code>PROPAGATION_REQUIRED</code>，那么执行<code>ServiceA.methodA()</code>的时候spring已经起了事务，这时调用<code>ServiceB.methodB()，ServiceB.methodB()</code>看到自己已经运行在<code>ServiceA.methodA()</code>的事务内部，就不再起新的事务。</p><p>假如<code>ServiceB.methodB()</code>运行的时候发现自己没有在事务中，他就会为自己分配一个事务。</p><p>这样，在<code>ServiceA.methodA()</code>或者在 <code>ServiceB.methodB()</code>内的任何地方出现异常，事务都会被回滚。</p><h3 id="PROPAGATION-REQUIRES-NEW"><a href="#PROPAGATION-REQUIRES-NEW" class="headerlink" title="PROPAGATION_REQUIRES_NEW"></a><code>PROPAGATION_REQUIRES_NEW</code></h3><p>比如我们设计<code>ServiceA.methodA()</code>的事务级别为<code>PROPAGATION_REQUIRED，</code>ServiceB.methodB()<code>的事务级别为</code>PROPAGATION_REQUIRES_NEW`。</p><p>那么当执行到<code>ServiceB.methodB()</code>的时候，<code>ServiceA.methodA()</code>所在的事务就会挂起，<code>ServiceB.methodB()</code>会起一个新的事务，等待<code>ServiceB.methodB()</code>的事务完成以后，它才继续执行。</p><p>他与<code>PROPAGATION_REQUIRED</code>的事务区别在于事务的回滚程度了。因为<code>ServiceB.methodB()</code>是新起一个事务，那么就是存在两个不同的事务。如果<code>ServiceB.methodB()</code>已经提交，那么<code>ServiceA.methodA()</code>失败回滚，<code>ServiceB.methodB()</code>是不会回滚的。如果<code>ServiceB.methodB()</code>失败回滚，如果他抛出的异常被<code>ServiceA.methodA()</code>捕获，<code>ServiceA.methodA()</code>事务仍然可能提交(主要看B抛出的异常是不是A会回滚的异常)。</p><h3 id="PROPAGATION-SUPPORTS"><a href="#PROPAGATION-SUPPORTS" class="headerlink" title="PROPAGATION_SUPPORTS"></a><code>PROPAGATION_SUPPORTS</code></h3><p>假设<code>ServiceB.methodB()</code>的事务级别为<code>PROPAGATION_SUPPORTS</code>，那么当执行到<code>ServiceB.methodB()</code>时，如果发现<code>ServiceA.methodA()</code>已经开启了一个事务，则加入当前的事务，如果发现<code>ServiceA.methodA()</code>没有开启事务，则自己也不开启事务。这种时候，内部方法的事务性完全依赖于最外层的事务。</p><h3 id="PROPAGATION-NESTED"><a href="#PROPAGATION-NESTED" class="headerlink" title="PROPAGATION_NESTED"></a><code>PROPAGATION_NESTED</code></h3><p>现在的情况就变得比较复杂了,<code>ServiceB.methodB()</code>的事务属性被配置为<code>PROPAGATION_NESTED</code>, 此时两者之间又将如何协作呢?<code>ServiceB#methodB</code>如果<code>rollback</code>, 那么内部事务(即 <code>ServiceB#methodB</code>) 将回滚到它执行前的<code>SavePoint</code>而外部事务(即<code>ServiceA#methodA</code>) 可以有以下两种处理方式:</p><p>a、捕获异常，执行异常分支逻辑</p><pre><code class="java">void methodA() {         try {             ServiceB.methodB();         } catch (SomeException) {             // 执行其他业务, 如 ServiceC.methodC();         }     }</code></pre><p>这种方式也是嵌套事务最有价值的地方, 它起到了分支执行的效果, 如果<code>ServiceB.methodB</code>失败, 那么执行<code>ServiceC.methodC()</code>, 而<code>ServiceB.methodB</code>已经回滚到它执行之前的<code>SavePoint</code>, 所以不会产生脏数据(相当于此方法从未执行过), 这种特性可以用在某些特殊的业务中, 而<code>PROPAGATION_REQUIRED</code>和<code>PROPAGATION_REQUIRES_NEW</code>都没有办法做到这一点。</p><p>b、外部事务回滚/提交 代码不做任何修改, 那么如果内部事务(<code>ServiceB#methodB</code>)<code>rollback</code>, 那么首先<code>ServiceB.methodB</code>回滚到它执行之前的<code>SavePoint</code>(在任何情况下都会如此), 外部事务(即<code>ServiceA#methodA</code>) 将根据具体的配置决定自己是<code>commit</code>还是<code>rollback</code></p><h1 id="七、总结"><a href="#七、总结" class="headerlink" title="七、总结"></a>七、总结</h1><p>对于项目中需要使用到事务的地方，我建议开发者还是使用spring的<code>TransactionCallback</code>接口来实现事务，不要盲目使用spring事务注解，如果一定要使用注解，那么一定要对spring事务的传播机制和隔离级别有个详细的了解，否则很可能发生意想不到的效果。</p><p>根据底层所使用的不同的持久化 API 或框架，使用如下：</p><ul><li><code>DataSourceTransactionManager</code>：适用于使用JDBC和iBatis进行数据持久化操作的情况，在定义时需要提供底层的数据源作为其属性，也就是<code>DataSource</code>。</li><li><code>HibernateTransactionManager</code>：适用于使用Hibernate进行数据持久化操作的情况，与<code>HibernateTransactionManager</code>对应的是<code>SessionFactory</code>。</li><li><code>JpaTransactionManager</code>：适用于使用JPA进行数据持久化操作的情况，与<code>JpaTransactionManager</code>对应的是<code>EntityManagerFactory</code>。</li></ul><blockquote><p>今天的努力，会成就明天的自己，努力，努力！</p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
      <tag>spring boot</tag>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>SQL优化</title>
    <link href="undefined2019/11/06/SQL%E4%BC%98%E5%8C%96/"/>
    <url>2019/11/06/SQL%E4%BC%98%E5%8C%96/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1563243777509.jpg" srcset="undefined" alt="u=671476709,2522611488fm=26gp=0.jpg"></p><ol><li><p>对查询进行优化，要尽量避免全表扫描，首先应考虑在<code>where</code>及<code>order by</code>涉及的列上建立索引。</p></li><li><p>应尽量避免在<code>where</code>子句中对字段进行<code>null</code>值判断，否则将导致引擎放弃使用索引而进行全表扫描，如：</p><pre><code class="sql">select id from t where num is null</code></pre><p>最好不要给数据库留<code>NULL</code>，尽可能的使用<code>NOT NULL</code>填充数据库.<br>备注、描述、评论之类的可以设置为<code>NULL</code>，其他的，最好不要使用<code>NULL</code>。<br>不要以为<code>NULL</code>不需要空间，比如：<code>char(100)</code>型，在字段建立时，空间就固定了， 不管是否插入值（<code>NULL</code>也包含在内），都是占用 100个字符的空间的，如果是<code>varchar</code>这样的变长字段，<code>null</code>不占用空间。<br>可以在num上设置默认值0，确保表中num列没有null值，然后这样查询：</p><pre><code class="sql">select id from t where num = 0</code></pre></li><li><p>应尽量避免在<code>where</code>子句中使用<code>!=</code>或<code>&lt;&gt;</code>操作符，否则将引擎放弃使用索引而进行全表扫描。</p></li><li><p>应尽量避免在<code>where</code>子句中使用<code>or</code>来连接条件，如果一个字段有索引，一个字段没有索引，将导致引擎放弃使用索引而进行全表扫描，如：</p><pre><code class="sql">select id from t where num=10 or Name = &#39;admin&#39;</code></pre><p>可以这样查询：</p><pre><code class="sql">select id from t where num = 10union allselect id from t where Name = &#39;admin&#39;</code></pre></li><li><p><code>in</code>和<code>not in</code>也要慎用，否则会导致全表扫描，如：</p><pre><code class="sql">select id from t where num in(1,2,3)</code></pre><p>对于连续的数值，能用 between 就不要用<code>in</code>了：</p><pre><code class="sql">select id from t where num between 1 and 3</code></pre><p>很多时候用<code>exists</code>代替<code>in</code>是一个好的选择：</p><pre><code class="sql">select num from a where num in(select num from b)</code></pre><p>用下面的语句替换：</p><pre><code class="sql">select num from a where exists(select 1 from b where num=a.num)</code></pre></li><li><p>下面的查询也将导致全表扫描：</p><pre><code class="sql">select id from t where name like ‘%abc%’</code></pre><p>若要提高效率，可以考虑全文检索。</p></li><li><p>如果在<code>where</code>子句中使用参数，也会导致全表扫描。因为SQL只有在运行时才会解析局部变量，但优化程序不能将访问计划的选择推迟到运行时；它必须在编译时进行选择。然 而，如果在编译时建立访问计划，变量的值还是未知的，因而无法作为索引选择的输入项。如下面语句将进行全表扫描：</p><pre><code class="sql">select id from t where num = @num</code></pre><p>可以改为强制查询使用索引：</p><pre><code class="sql">select id from t with(index(索引名)) where num = @num</code></pre><p>应尽量避免在 where 子句中对字段进行表达式操作，这将导致引擎放弃使用索引而进行全表扫描。如：</p><pre><code class="sql">select id from t where num/2 = 100</code></pre><p>应改为:</p><pre><code class="sql">select id from t where num = 100*2</code></pre></li><li><p>应尽量避免在<code>where</code>子句中对字段进行函数操作，这将导致引擎放弃使用索引而进行全表扫描。如：</p><pre><code class="sql">select id from t where substring(name,1,3) = ’abc’       -–name以abc开头的idselect id from t where datediff(day,createdate,’2005-11-30′) = 0    -–‘2005-11-30’    --生成的id</code></pre><p>应改为:</p><pre><code class="sql">select id from t where name like &#39;abc%&#39;select id from t where createdate &gt;= &#39;2005-11-30&#39; and createdate &lt; &#39;2005-12-1&#39;</code></pre></li><li><p>不要在<code>where</code>子句中的<code>=</code>左边进行函数、算术运算或其他表达式运算，否则系统将可能无法正确使用索引。</p></li><li><p>在使用索引字段作为条件时，如果该索引是复合索引，那么必须使用到该索引中的第一个字段作为条件时才能保证系统使用该索引，否则该索引将不会被使用，并且应尽可能的让字段顺序与索引顺序相一致。</p></li><li><p>不要写一些没有意义的查询，如需要生成一个空表结构：</p><pre><code class="sql">select col1,col2 into #t from t where 1=0</code></pre><p>这类代码不会返回任何结果集，但是会消耗系统资源的，应改成这样：</p><pre><code class="sql">create table #t(…)</code></pre></li><li><p><code>Update</code>语句，如果只更改1、2个字段，不要<code>Update</code>全部字段，否则频繁调用会引起明显的性能消耗，同时带来大量日志。</p></li><li><p>对于多张大数据量（这里几百条就算大了）的表<code>JOIN</code>，要先分页再<code>JOIN</code>，否则逻辑读会很高，性能很差。</p></li><li><p><code>select count(*) from table；</code>这样不带任何条件的<code>count</code>会引起全表扫描，并且没有任何业务意义，是一定要杜绝的。</p></li><li><p>索引并不是越多越好，索引固然可以提高相应的<code>select</code>的效率，但同时也降低了<code>insert</code>及<code>update</code>的效率，因为<code>insert</code>或<code>update</code>时有可能会重建索引，所以怎样建索引需要慎重考虑，视具体情况而定。一个表的索引数最好不要超过6个，若太多则应考虑一些不常使用到的列上建的索引是否有 必要。</p></li><li><p>应尽可能的避免更新<code>clustered</code>索引数据列，因为<code>clustered</code>索引数据列的顺序就是表记录的物理存储顺序，一旦该列值改变将导致整个表记录的顺序的调整，会耗费相当大的资源。若应用系统需要频繁更新<code>clustered</code>索引数据列，那么需要考虑是否应将该索引建为<code>clustered</code>索引。</p></li><li><p>尽量使用数字型字段，若只含数值信息的字段尽量不要设计为字符型，这会降低查询和连接的性能，并会增加存储开销。这是因为引擎在处理查询和连 接时会逐个比较字符串中每一个字符，而对于数字型而言只需要比较一次就够了。</p></li><li><p>尽可能的使用<code>varchar/nvarchar</code>代替<code>char/nchar</code>，因为首先变长字段存储空间小，可以节省存储空间，其次对于查询来说，在一个相对较小的字段内搜索效率显然要高些。</p></li><li><p>任何地方都不要使用<code>select * from t</code>用具体的字段列表代替<code>*</code>，不要返回用不到的任何字段。</p></li><li><p>尽量使用表变量来代替临时表。如果表变量包含大量数据，请注意索引非常有限（只有主键索引）。</p></li><li><p>避免频繁创建和删除临时表，以减少系统表资源的消耗。临时表并不是不可使用，适当地使用它们可以使某些例程更有效，例如，当需要重复引用大型表或常用表中的某个数据集时。但是，对于一次性事件， 最好使用导出表。</p></li><li><p>在新建临时表时，如果一次性插入数据量很大，那么可以使用<code>select into</code>代替<code>create table</code>，避免造成大量 log ，以提高速度；如果数据量不大，为了缓和系统表的资源，应先<code>create table</code>，然后<code>insert</code>。</p></li><li><p>如果使用到了临时表，在存储过程的最后务必将所有的临时表显式删除，先<code>truncate table</code>，然后<code>drop table</code>，这样可以避免系统表的较长时间锁定。</p></li><li><p>尽量避免使用游标，因为游标的效率较差，如果游标操作的数据超过1万行，那么就应该考虑改写。</p></li><li><p>使用基于游标的方法或临时表方法之前，应先寻找基于集的解决方案来解决问题，基于集的方法通常更有效。</p></li><li><p>与临时表一样，游标并不是不可使用。对小型数据集使用<code>FAST_FORWARD</code>游标通常要优于其他逐行处理方法，尤其是在必须引用几个表才能获得所需的数据时。在结果集中包括“合计”的例程通常要比使用游标执行的速度快。如果开发时 间允许，基于游标的方法和基于集的方法都可以尝试一下，看哪一种方法的效果更好。</p></li><li><p>在所有的存储过程和触发器的开始处设置<code>SET NOCOUNT ON</code>，在结束时设置<code>SET NOCOUNT OFF</code>。无需在执行存储过程和触发器的每个语句后向客户端发送<code>DONE_IN_PROC</code>消息。</p></li><li><p>尽量避免大事务操作，提高系统并发能力。</p></li><li><p>尽量避免向客户端返回大数据量，若数据量过大，应该考虑相应需求是否合理。</p></li></ol><blockquote><p>人所缺乏的不是才干而是志向，不是成功的能力而是勤劳的意志。 —— 部尔卫</p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>sql</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java并发编程了解一下</title>
    <link href="undefined2019/11/06/java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E4%BA%86%E8%A7%A3%E4%B8%80%E4%B8%8B/"/>
    <url>2019/11/06/java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E4%BA%86%E8%A7%A3%E4%B8%80%E4%B8%8B/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1563332775154.jpg" srcset="undefined" alt="u=2054456090,108871660fm=26gp=0.jpg"></p><p>在Java并发编程中，经常遇到多个线程访问同一个 共享资源 ，这时候作为开发者必须考虑<strong>如何维护数据一致性，这就是Java锁机制(线程同步)的来源。</strong></p><h3 id="Java提供了多种多线程锁机制的实现方式，常见的有："><a href="#Java提供了多种多线程锁机制的实现方式，常见的有：" class="headerlink" title="Java提供了多种多线程锁机制的实现方式，常见的有："></a>Java提供了多种多线程锁机制的实现方式，常见的有：</h3><ol><li><p>synchronized</p></li><li><p>ReentrantLock</p></li><li><p>Semaphore</p></li><li><p>AtomicInteger等</p></li></ol><p>每种机制都有优缺点与各自的适用场景，必须熟练掌握他们的特点才能在Java多线程应用开发时得心应手。</p><h1 id="4种Java线程锁"><a href="#4种Java线程锁" class="headerlink" title="4种Java线程锁"></a>4种Java线程锁</h1><h3 id="1-synchronized"><a href="#1-synchronized" class="headerlink" title="1. synchronized"></a>1. synchronized</h3><p>在Java中<code>synchronized</code>关键字被常用于维护数据一致性。</p><p><code>synchronized</code>机制是给共享资源上锁，只有拿到锁的线程才可以访问共享资源，这样就可以强制使得对共享资源的访问都是顺序的。</p><p>Java开发人员都认识<code>synchronized</code>，使用它来实现多线程的同步操作是非常简单的，只要在需要同步的对方的方法、类或代码块中加入该关键字，它能够保证在同一个时刻最多只有一个线程执行同一个对象的同步代码，可保证修饰的代码在执行过程中不会被其他线程干扰。</p><p>使用<code>synchronized</code>修饰的代码具有原子性和可见性，在需要进程同步的程序中使用的频率非常高，可以满足一般的进程同步要求。</p><ol><li><p>原子性：原子，即一个不可再被分割的颗粒。在Java中原子性指的是一个或多个操作要么全部执行成功要么全部执行失败。</p></li><li><p>有序性：程序执行的顺序按照代码的先后顺序执行。（处理器可能会对指令进行重排序）</p></li><li><p>可见性：当多个线程访问同一个变量时，如果其中一个线程对其作了修改，其他线程能立即获取到最新的值。</p></li></ol><p><code>synchronized</code>实现的机理依赖于软件层面上的JVM，因此其性能会随着Java版本的不断升级而提高。到了Java1.6，<code>synchronized</code>进行了很多的优化，有适应自旋、锁消除、锁粗化、轻量级锁及偏向锁等，效率有了本质上的提高。在之后推出的Java1.7与1.8中，均对该关键字的实现机理做了优化。需要说明的是，当线程通过<code>synchronized</code>等待锁时是不能被<code>Thread.interrupt()</code>中断的，因此程序设计时必须检查确保合理，否则可能会造成线程死锁的尴尬境地。最后，尽管Java实现的锁机制有很多种，并且有些锁机制性能也比<code>synchronized</code>高，但还是强烈推荐在多线程应用程序中使用该关键字，因为实现方便，后续工作由JVM来完成，可靠性高。只有在确定锁机制是当前多线程程序的性能瓶颈时，才考虑使用其他机制，如<code>ReentrantLock</code>等。</p><h3 id="2-ReentrantLock"><a href="#2-ReentrantLock" class="headerlink" title="2. ReentrantLock"></a>2. ReentrantLock</h3><p>可重入锁，顾名思义，这个锁可以被线程多次重复进入进行获取操作。<code>ReentantLock</code>继承接口<code>Lock</code>并实现了接口中定义的方法，除了能完成<code>synchronized</code>所能完成的所有工作外，还提供了诸如可响应中断锁、可轮询锁请求、定时锁等避免多线程死锁的方法。<code>Lock</code>实现的机理依赖于特殊的CPU指定，可以认为不受JVM的约束，并可以通过其他语言平台来完成底层的实现。在并发量较小的多线程应用程序中，<code>ReentrantLock</code>与<code>synchronized</code>性能相差无几，但在高并发量的条件下，synchronized性能会迅速下降几十倍，而<code>ReentrantLock</code>的性能却能依然维持一个水准。因此建议在高并发量情况下使用<code>ReentrantLock</code>。</p><p>ReentrantLock引入两个概念：<strong>公平锁与非公平锁。</strong></p><p>公平锁指的是锁的分配机制是公平的，通常先对锁提出获取请求的线程会先被分配到锁。反之，JVM按随机、就近原则分配锁的机制则称为不公平锁。</p><p><code>ReentrantLock</code>在构造函数中提供了是否公平锁的初始化方式，默认为非公平锁。这是因为，非公平锁实际执行的效率要远远超出公平锁，除非程序有特殊需要，否则最常用非公平锁的分配机制。<code>ReentrantLock</code>通过方法<code>lock()</code>与<code>unlock()</code>来进行加锁与解锁操作，与<code>synchronized</code>会被JVM自动解锁机制不同，<code>ReentrantLock</code>加锁后需要手动进行解锁。为了避免程序出现异常而无法正常解锁的情况，使用<code>ReentrantLock</code>必须在<code>finally</code>控制块中进行解锁操作。通常使用方式如下所示：</p><pre><code class="java">Lock lock = new ReentrantLock();try {lock.lock();//...进行任务操作5 }finally {lock.unlock();}</code></pre><h3 id="3-Semaphore"><a href="#3-Semaphore" class="headerlink" title="3. Semaphore"></a>3. Semaphore</h3><p>上述两种锁机制类型都是<strong>互斥锁</strong>，学过操作系统的都知道，互斥是进程同步关系的一种特殊情况，相当于只存在一个临界资源，因此同时最多只能给一个线程提供服务。但是，在实际复杂的多线程应用程序中，可能存在多个临界资源，这时候我们可以借助<code>Semaphore</code>信号量来完成多个临界资源的访问。<code>Semaphore</code>基本能完成<code>ReentrantLock</code>的所有工作，使用方法也与之类似，通过<code>acquire()</code>与<code>release()</code>方法来获得和释放临界资源。经实测，<code>Semaphone.acquire()</code>方法默认为可响应中断锁，与<code>ReentrantLock.lockInterruptibly()</code>作用效果一致，也就是说在等待临界资源的过程中可以被<code>Thread.interrupt()</code>方法中断。此外，<code>Semaphore</code>也实现了可轮询的锁请求与定时锁的功能，除了方法名<code>tryAcquire</code>与<code>tryLock</code>不同，其使用方法与<code>ReentrantLock</code>几乎一致。<code>Semaphore</code>也提供了公平与非公平锁的机制，也可在构造函数中进行设定。<code>Semaphore</code>的锁释放操作也由手动进行，因此与<code>ReentrantLock</code>一样，为避免线程因抛出异常而无法正常释放锁的情况发生，释放锁的操作也必须在<code>finally</code>代码块中完成。</p><h3 id="4-AtomicInteger"><a href="#4-AtomicInteger" class="headerlink" title="4. AtomicInteger"></a>4. AtomicInteger</h3><p>首先说明，此处<code>AtomicInteger</code>是一系列相同类的代表之一，常见的还有<code>AtomicLong、AtomicLong</code>等，他们的实现原理相同，区别在与运算对象类型的不同。我们知道，在多线程程序中，诸如++i 或 i++等运算不具有原子性，是不安全的线程操作之一。通常我们会使用<code>synchronized</code>将该操作变成一个原子操作，但JVM为此类操作特意提供了一些同步类，使得使用更方便，且使程序运行效率变得更高。通过相关资料显示，通常<code>AtomicInteger</code>的性能是<code>ReentantLock</code>的好几倍。</p><h1 id="Java线程锁总结"><a href="#Java线程锁总结" class="headerlink" title="Java线程锁总结"></a>Java线程锁总结</h1><h3 id="1-synchronized-1"><a href="#1-synchronized-1" class="headerlink" title="1. synchronized"></a>1. synchronized</h3><p>在资源竞争不是很激烈的情况下，偶尔会有同步的情形下，<code>synchronized</code>是很合适的。原因在于，编译程序通常会尽可能的进行优化<code>synchronize</code>，另外可读性非常好。</p><h3 id="2-ReentrantLock-1"><a href="#2-ReentrantLock-1" class="headerlink" title="2. ReentrantLock"></a>2. ReentrantLock</h3><p>在资源竞争不激烈的情形下，性能稍微比<code>synchronized</code>差点点。但是当同步非常激烈的时候，<code>synchronized</code>的性能一下子能下降好几十倍，而<code>ReentrantLock</code>确还能维持常态。</p><p><strong>高并发量情况下使用<code>ReentrantLock</code>。</strong></p><h3 id="3-Atomic"><a href="#3-Atomic" class="headerlink" title="3. Atomic"></a>3. Atomic</h3><p>不激烈情况下，性能比<code>synchronized</code>略逊，而激烈的时候，也能维持常态。激烈的时候，<code>Atomic</code>的性能会优于<code>ReentrantLock</code>一倍左右。</p><p>但是其有一个缺点，就是只能同步一个值，一段代码中只能出现一个<code>Atomic</code>的变量，多于一个同步无效,因为他不能在多个<code>Atomic</code>之间同步。</p><p>所以，我们写同步的时候，优先考虑<code>synchronized</code>，如果有特殊需要，再进一步优化。<code>ReentrantLock</code>和<code>Atomic</code>如果用的不好，不仅不能提高性能，还可能带来灾难。</p><h1 id="Thread-类的常用函数及功能"><a href="#Thread-类的常用函数及功能" class="headerlink" title="Thread 类的常用函数及功能:"></a>Thread 类的常用函数及功能:</h1><h3 id="1-sleep"><a href="#1-sleep" class="headerlink" title="1. sleep():"></a>1. sleep():</h3><p><code>sleep</code>是<code>Thread</code>的静态方法，使当前的正在执行线程处于停滞状态，<code>sleep()</code>使线程进入堵塞状态，同时不会释放所资源，<code>sleep</code>可使优先级低的线程得到执行的机会，当然也可以让同优先级和高优先级的线程有执行的机会。</p><h3 id="2-wait"><a href="#2-wait" class="headerlink" title="2. wait():"></a>2. wait():</h3><p><code>wait</code>使当前线程处于等待状态，会释放当前的锁资源，使用<code>wait()</code>的时候要注意：</p><p><code>wait()、notify()、notifyAll()</code>都必须在<code>synchronized</code>中执行，否则会抛出异常</p><p><code>wait()、notify()、notifyAll()</code>都是属于超类<code>Object</code>的方法 一个对象只有一个锁（对象锁和类锁还是有区别的）</p><p><code>wait()</code>和<code>sleep()</code>区别：<br>a) <code>wait()</code>可以不指定时间，<code>sleep()</code>必须指定时间<br>b) <code>wait()</code>会释放当前锁资源,<code>sleep()</code>不能够释放锁资源<br>c) <code>wait()</code>是来自<code>Object</code>类中,<code>sleep()</code>是来自<code>Thread</code>类</p><h3 id="3-join"><a href="#3-join" class="headerlink" title="3. join():"></a>3. join():</h3><p><code>join</code>是让当前线程等待调用<code>join</code>的线程运行完<code>run</code>方法，可以指定时间，若指定了时间，则等待指定时间，即便调用<code>join</code>的线程没运行完<code>run</code>方法，当前线程也会继续往下运行；若未指定时间，则当前线程一直等待，直到<code>join</code>的线程运行完<code>run</code>方法。</p><h3 id="4-yield-："><a href="#4-yield-：" class="headerlink" title="4. yield()："></a>4. yield()：</h3><p><code>yield</code>也是<code>Thread</code>的静态方法，<code>yield</code>的本质就是将当前线程重新放入抢占CPU时间的<strong>队列</strong>中，当前线程愿意让出CPU的使用权,可以让其他线程继续执行，但是线程调度器可能会停止当前线程继续执行,也可能会让该线程继续执行。</p><p>并且与线程优先级并无关系，优先级高的不一定先执行。线程的优先级将该线程的重要性传递给线程调度器，调度器将倾向于让优先权最高的线程先执行.然后这并不意味值优先权较低的线程将得不到执行。优先级较低的线程仅仅是执行的频率较低。</p><h3 id="5-stop"><a href="#5-stop" class="headerlink" title="5. stop():"></a>5. stop():</h3><p>可以停止正在执行得线程，这样的方法不安全，不建议使用。他会解除线程获取的所有锁定，如果线程处于一种不连贯的状态，其他线程有可能在那种状态下检查和修改他们，很难找到问题的所在。</p><h3 id="6-suspend"><a href="#6-suspend" class="headerlink" title="6. suspend():"></a>6. suspend():</h3><p>容易发生死锁，调用<code>suspend()</code>的时候，目标线程会停止，但是却仍然有之前获取的锁定。此时，其他线程都不能有访问线程锁定的资源，除非被<strong>挂起</strong>的线程恢复运行。需要在<code>Thread</code>类中有一个标志。</p><p>若标志指出线程应该挂起，便用<code>wait()</code>命其进入等待状态。若标志指出线程应当恢复，则用一个<code>notify()</code>重新启动线程。</p><h3 id="7-interrupt-："><a href="#7-interrupt-：" class="headerlink" title="7. interrupt()："></a>7. interrupt()：</h3><p><code>interrupt()</code>不会中断一个正在运行的线程。这一方法实际上完成的是：在线程受到阻塞时抛出一个中断信号，线程就得以退出阻塞的状态。<br>如果线程被<code>Object.wait，Thread.join，Thread.sleep</code>三种方法之一阻塞，那么，它将接收到一个中断异（InterruptedException），从而提早地终结被阻塞状态。<br>如果线程没有被阻塞，这时调用<code>interrupt()</code>将不起作用。</p><h3 id="8-setPriority"><a href="#8-setPriority" class="headerlink" title="8. setPriority():"></a>8. setPriority():</h3><p>线程分配时间片的多少就决定线程使用处理的多少，刚好对应线程优先级别这个概念。可以通过int priority()，里边可以填1-10，默认为5，10最高。</p><h1 id="Java中的volatile"><a href="#Java中的volatile" class="headerlink" title="Java中的volatile"></a>Java中的volatile</h1><p><code>volatile</code>是Java提供的一种轻量级的同步机制，在并发编程中，它也扮演着比较重要的角色。同<code>synchronized</code>相比（<code>synchronized</code>通常称为重量级锁），<code>volatile</code>更轻量级，相比使用<code>synchronized</code>所带来的庞大开销，使用<code>synchronized</code> 虽然可以解决多线程安全问题，但弊端也很明显：加锁后多个线程需要判断锁，较为消耗资源。</p><p><code>volatile</code>关键字作用</p><ul><li>内存可见性</li><li>禁止指令重排</li></ul><p>所谓可见性，是指当一条线程修改了共享变量的值，新值对于其他线程来说是可以立即得知的。java虚拟机有自己的内存模型（Java Memory Model，JMM），JMM可以屏蔽掉各种硬件和操作系统的内存访问差异，以实现让java程序在各种平台下都能达到一致的内存访问效果。JMM决定一个线程对共享变量的写入何时对另一个线程可见，JMM定义了线程和主内存之间的抽象关系：共享变量存储在主内存(Main Memory)中，每个线程都有一个私有的本地内存（Local Memory），本地内存保存了被该线程使用到的主内存的副本拷贝，线程对变量的所有操作都必须在工作内存中进行，而不能直接读写主内存中的变量。这三者之间的交互关系如下：<br><img src="https://user-gold-cdn.xitu.io/2019/7/7/16bcb272344a798b?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" srcset="undefined" alt="关系图"></p><p>需要注意的是，JMM是个抽象的内存模型，所以所谓的本地内存，主内存都是抽象概念，并不一定就真实的对应cpu缓存和物理内存。Java 中存在一种原则——先行发生原则（happens-before）。其表示两个事件结果之间的关系：如果一个事件发生在另一个事件之间，其结果必须体现。<code>volatile</code>的内存可见性就体现了该原则：对于一个<code>volatile</code>变量的写操作先行发生于后面对这个变量的读操作。将一个共享变量声明为<code>volatile</code>后，会有以下效应：</p><ol><li>当写一个volatile变量时，JMM会把该线程对应的本地内存中的变量强制刷新到主内存中去；</li><li>这个写会操作会导致其他线程中的缓存无效。</li></ol><p>但是需要注意的是，我们一直在拿volatile和synchronized做对比，仅仅是因为这两个关键字在某些内存语义上有共通之处，volatile并不能完全替代synchronized，它依然是个轻量级锁，在很多场景下，volatile并不能胜任。</p><p>例如：当多个线程都对某一 volatile 变量（int a=0）进行 count++ 操作时，由于 count++ 操作并不是原子性操作，当线程 A 执行 count++ 后，A 工作内存其副本的值为 1，但线程执行时间到了，主内存的值仍为 0 ；线程 B又来执行 count++后并将值更新到主内存，主内存此时的值为 1；然后线程 A 继续执行将值更新到主内存为 1，它并不知道线程 B 对变量进行了修改，也就是没有判断主内存的值是否发生改变，故最终结果为 1，但理论上 count++ 两次，值应该为 2。</p><p>所以要使用 volatile 的内存可见性特性的话得满足两个条件：</p><p>能确保只有单一的线程对共享变量的只进行修改。</p><p>变量不需要和其他状态变量共同参与不变的约束条件。</p><p>所谓重排序，是指编译器和处理器为了优化程序性能而对指令序列进行排序的一种手段。但是重排序也需要遵守一定规则：</p><p><strong>1. 重排序操作不会对存在数据依赖关系的操作进行重排序。</strong></p><p>比如：a=1;b=a; 这个指令序列，由于第二个操作依赖于第一个操作，所以在编译时和处理器运行时这两个操作不会被重排序。</p><p><strong>2. 重排序是为了优化性能，但是不管怎么重排序，单线程下程序的执行结果不能被改变</strong></p><p>比如：a=1;b=2;c=a+b这三个操作，第一步（a=1)和第二步(b=2)由于不存在数据依赖关系，所以可能会发生重排序，但是c=a+b这个操作是不会被重排序的，因为需要保证最终的结果一定是c=a+b=3。</p><p>重排序在单线程模式下是一定会保证最终结果的正确性，但是在多线程环境下，问题就出来了。指令重排虽说可以优化程序的执行效率，但在多线程问题上会影响结果。那么有什么解决办法呢？答案是内存屏障。</p><p>内存屏障是一种屏障指令，使 CPU 或编译器对屏障指令之前和之后发出的内存操作执行一个排序的约束。</p><p>四种类型：LoadLoad 屏障、StoreStore 屏障、LoadStore 屏障、StoreLoad 屏障。（Load 代表读取指令、Store 代表写入操作）</p><p>在 volatile 变量上的体现：</p><p>（JVM 执行操作） 在每个 volatile 写入操作前插入 StoreStore 屏障；</p><p>在写操作后插入 StoreLoad 屏障；</p><p>在读操作前插入 LoadLoad 屏障；</p><p>在读操作后插入 LoadStore 屏障；</p><p>简单总结下，volatile是一种轻量级的同步机制，它主要有两个特性：一是保证共享变量对所有线程的可见性；二是禁止指令重排序优化。同时需要注意的是，volatile对于单个的共享变量的读/写具有原子性，但是像num++这种复合操作，volatile无法保证其原子性。</p><h1 id="可重入锁与不可重入锁"><a href="#可重入锁与不可重入锁" class="headerlink" title="可重入锁与不可重入锁"></a>可重入锁与不可重入锁</h1><p>所谓重入锁，指的是以线程为单位，当一个线程获取对象锁之后，这个线程可以再次获取本对象上的锁，而其他的线程是不可以的。</p><p>synchronized 和 ReentrantLock 都是可重入锁。</p><p>可重入锁的意义在于防止死锁。</p><p>实现原理是通过为每个锁关联一个请求计数器和一个占有它的线程。当计数为0时，认为锁是未被占有的；线程请求一个未被占有的锁时，JVM将记录锁的占有者，并且将请求计数器置为1 。</p><p>如果同一个线程再次请求这个锁，计数将递增；</p><p>每次占用线程退出同步块，计数器值将递减。直到计数器为0,锁被释放。</p><p>关于父类和子类的锁的重入:子类覆写了父类的synchonized方法，然后调用父类中的方法，此时如果没有重入的锁，那么这段代码将产生死锁（很好理解吧）。</p><p>例子：<br>比如说A类中有个方法</p><pre><code class="java">public synchronized methodA1(){methodA2();}而且public synchronized methodA2(){//具体操作}</code></pre><p>也是A类中的同步方法，当当前线程调用A类的对象methodA1同步方法，如果其他线程没有获取A类的对象锁，那么当前线程就获得当前A类对象的锁，然后执行methodA1同步方法，方法体中调用methodA2同步方法，当前线程能够再次获取A类对象的锁，而其他线程是不可以的，这就是可重入锁。</p><p>不可重入锁：</p><pre><code class="java">public class Lock{        private boolean isLocked = false;        public synchronized void lock() throws InterruptedException    {                while(isLocked){                            wait();        }        isLocked = true;    }    public synchronized void unlock()    {        isLocked = false;        notify();    }}</code></pre><p>使用该锁：</p><pre><code class="java">public class Count{    Lock lock = new Lock();    public void print(){        lock.lock();        doAdd();        lock.unlock();    }    public void doAdd(){        lock.lock();        //do something        lock.unlock();    }}</code></pre><p>当前线程执行print()方法首先获取lock，接下来执行doAdd()方法就无法执行doAdd()中的逻辑，必须先释放锁。这个例子很好的说明了不可重入锁。</p><blockquote><p>勇敢坚毅真正之才智乃刚毅之志向。 —— 拿破仑</p></blockquote><blockquote><p>出处：<a href="https://juejin.im/post/5cf3ad4c518825623928f10e" target="_blank" rel="noopener">Ai布可思咦</a></p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java多线程之悲观锁与乐观锁</title>
    <link href="undefined2019/11/06/java%E5%A4%9A%E7%BA%BF%E7%A8%8B%E4%B9%8B%E6%82%B2%E8%A7%82%E9%94%81%E4%B8%8E%E4%B9%90%E8%A7%82%E9%94%81/"/>
    <url>2019/11/06/java%E5%A4%9A%E7%BA%BF%E7%A8%8B%E4%B9%8B%E6%82%B2%E8%A7%82%E9%94%81%E4%B8%8E%E4%B9%90%E8%A7%82%E9%94%81/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1564623549502.jpg" srcset="undefined" alt="rBgICV0dxGKAfOz3AABxYoZmlcc385.jpg"></p><h3 id="一、悲观锁"><a href="#一、悲观锁" class="headerlink" title="一、悲观锁"></a>一、悲观锁</h3><p>总是假设最坏的情况，每次去拿数据的时候都认为别人会修改，所以每次在拿数据的时候都会上锁，这样别人想拿这个数据就会阻塞直到它拿到锁（共享资源每次只给一个线程使用，其它线程阻塞，用完后再把资源转让给其它线程）。传统的关系型数据库里边就用到了很多这种锁机制，比如行锁，表锁等，读锁，写锁等，都是在做操作之前先上锁。Java中<code>synchronized</code>和<code>ReentrantLock</code>等独占锁就是悲观锁思想的实现。</p><h3 id="二、乐观锁"><a href="#二、乐观锁" class="headerlink" title="二、乐观锁"></a>二、乐观锁</h3><p>总是假设最好的情况，每次去拿数据的时候都认为别人不会修改，所以不会上锁，但是在更新的时候会判断一下在此期间别人有没有去更新这个数据，可以使用版本号机制和CAS算法实现。乐观锁适用于多读的应用类型，这样可以提高吞吐量，像数据库提供的类似于<code>write_condition</code>机制，其实都是提供的乐观锁。在Java中<code>java.util.concurrent.atomic</code>包下面的原子变量类就是使用了乐观锁的一种实现方式CAS实现的。</p><h3 id="三、两种锁的使用场景"><a href="#三、两种锁的使用场景" class="headerlink" title="三、两种锁的使用场景"></a>三、两种锁的使用场景</h3><p>从上面对两种锁的介绍，我们知道两种锁各有优缺点，不可认为一种好于另一种，像乐观锁适用于写比较少的情况下（多读场景），即冲突真的很少发生的时候，这样可以省去了锁的开销，加大了系统的整个吞吐量。但如果是多写的情况，一般会经常产生冲突，这就会导致上层应用会不断的进行retry，这样反倒是降低了性能，所以一般多写的场景下用悲观锁就比较合适。</p><h3 id="四、乐观锁常见的两种实现方式"><a href="#四、乐观锁常见的两种实现方式" class="headerlink" title="四、乐观锁常见的两种实现方式"></a>四、乐观锁常见的两种实现方式</h3><p>乐观锁一般会使用版本号机制或CAS（Compare-and-Swap，即比较并替换）算法实现。</p><h4 id="4-1-版本号机制"><a href="#4-1-版本号机制" class="headerlink" title="4.1 版本号机制"></a>4.1 版本号机制</h4><p>一般是在数据表中加上一个数据版本号version字段，表示数据被修改的次数，当数据被修改时，version值会加一。当线程A要更新数据值时，在读取数据的同时也会读取version值，在提交更新时，若刚才读取到的version值为当前数据库中的version值相等时才更新，否则重试更新操作，直到更新成功。</p><p>举一个简单的例子：假设数据库中帐户信息表中有一个version字段，当前值为1；而当前帐户余额字段（balance）为$100。</p><ol><li><p>操作员A此时将其读出（version=1）,并从其帐户余额中扣除$50（$100-$50）。</p></li><li><p>在操作员A操作的过程中，操作员B也读入此用户信息（version=1），并从其帐户余额中扣除 $20（$100-$20）。</p></li><li><p>操作员A完成了修改工作，将数据版本号加一（version=2），连同帐户扣除后余额（balance=$50），提交至数据库更新，此时由于提交数据版本大于数据库记录当前版本，数据被更新，数据库记录version更新为2。</p></li><li><p>操作员B完成了操作，也将版本号加一（version=2）试图向数据库提交数据（balance=$80），但此时比对数据库记录版本时发现，操作员B提交的数据版本号为2，数据库记录当前版本也为2，不满足<strong>提交版本必须大于记录当前版本才能执行更新</strong>的乐观锁策略，因此，操作员B的提交被驳回。<br>这样，就避免了操作员B用基于version=1的旧数据修改的结果覆盖操作员A 的操作结果的可能。</p></li></ol><h4 id="4-2-CAS算法"><a href="#4-2-CAS算法" class="headerlink" title="4.2 CAS算法"></a>4.2 CAS算法</h4><p>即 compare and swap（比较与交换），是一种有名的无锁算法。无锁编程，即不使用锁的情况下实现多线程之间的变量同步，也就是在没有线程被阻塞的情况下实现变量的同步，所以也叫非阻塞同步（Non-blocking Synchronization）。CAS算法涉及到三个操作数</p><ul><li>需要读写的内存值 V</li><li>进行比较的值 A</li><li>拟写入的新值 B</li></ul><p>当且仅当 V 的值等于 A时，CAS通过原子方式用新值B来更新V的值，否则不会执行任何操作（比较和替换是一个原子操作）。一般情况下是一个自旋操作，即不断的重试。</p><p>关于自旋锁，大家可以看一下这篇文章，非常不错：《 面试必备之深入理解自旋锁》</p><h3 id="五、乐观锁的缺点"><a href="#五、乐观锁的缺点" class="headerlink" title="五、乐观锁的缺点"></a>五、乐观锁的缺点</h3><p>ABA 问题是乐观锁一个常见的问题。</p><h4 id="1-ABA-问题"><a href="#1-ABA-问题" class="headerlink" title="1. ABA 问题"></a>1. ABA 问题</h4><p>如果一个变量V初次读取的时候是A值，并且在准备赋值的时候检查到它仍然是A值，那我们就能说明它的值没有被其他线程修改过了吗？很明显是不能的，因为在这段时间它的值可能被改为其他值，然后又改回A，那CAS操作就会误认为它从来没有被修改过。这个问题被称为CAS操作的 “ABA”问题。</p><p>JDK 1.5 以后的 AtomicStampedReference 类就提供了此种能力，其中的 compareAndSet 方法就是首先检查当前引用是否等于预期引用，并且当前标志是否等于预期标志，如果全部相等，则以原子方式将该引用和该标志的值设置为给定的更新值。</p><h4 id="2-循环时间长开销大"><a href="#2-循环时间长开销大" class="headerlink" title="2. 循环时间长开销大"></a>2. 循环时间长开销大</h4><p>自旋CAS（也就是不成功就一直循环执行直到成功）如果长时间不成功，会给CPU带来非常大的执行开销。 如果JVM能支持处理器提供的pause指令那么效率会有一定的提升，pause指令有两个作用，第一它可以延迟流水线执行指令（de-pipeline）,使CPU不会消耗过多的执行资源，延迟的时间取决于具体实现的版本，在一些处理器上延迟时间是零。第二它可以避免在退出循环的时候因内存顺序冲突（memory order violation）而引起CPU流水线被清空（CPU pipeline flush），从而提高CPU的执行效率。</p><h4 id="3-只能保证一个共享变量的原子操作"><a href="#3-只能保证一个共享变量的原子操作" class="headerlink" title="3. 只能保证一个共享变量的原子操作"></a>3. 只能保证一个共享变量的原子操作</h4><p>CAS 只对单个共享变量有效，当操作涉及跨多个共享变量时 CAS 无效。但是从 JDK 1.5开始，提供了AtomicReference类来保证引用对象之间的原子性，你可以把多个变量放在一个对象里来进行 CAS 操作.所以我们可以使用锁或者利用AtomicReference类把多个共享变量合并成一个共享变量来操作。</p><h3 id="六、CAS与synchronized的使用情景"><a href="#六、CAS与synchronized的使用情景" class="headerlink" title="六、CAS与synchronized的使用情景"></a>六、CAS与<code>synchronized</code>的使用情景</h3><p>简单的来说CAS适用于写比较少的情况下（多读场景，冲突一般较少），<code>synchronized</code>适用于写比较多的情况下（多写场景，冲突一般较多）</p><ul><li>对于资源竞争较少（线程冲突较轻）的情况，使用<code>synchronized</code>同步锁进行线程阻塞和唤醒切换以及用户态内核态间的切换操作额外浪费消耗cpu资源；而CAS基于硬件实现，不需要进入内核，不需要切换线程，操作自旋几率较少，因此可以获得更高的性能。</li><li>对于资源竞争严重（线程冲突严重）的情况，CAS自旋的概率会比较大，从而浪费更多的CPU资源，效率低于<code>synchronized</code>。</li></ul><p>补充： Java并发编程这个领域中<code>synchronized</code>关键字一直都是元老级的角色，很久之前很多人都会称它为<strong>重量级锁</strong>。但是，在JavaSE1.6之后进行了主要包括为了减少获得锁和释放锁带来的性能消耗而引入的 偏向锁 和 轻量级锁 以及其它各种优化之后变得在某些情况下并不是那么重了。<code>synchronized</code>的底层实现主要依靠<code>Lock-Free</code>的队列，基本思路是 自旋后阻塞，竞争切换后继续竞争锁，稍微牺牲了公平性，但获得了高吞吐量。在线程冲突较少的情况下，可以获得和CAS类似的性能；而线程冲突严重的情况下，性能远高于CAS。</p><blockquote><p>生活有度，人生添寿。 —— 书摘</p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Vue-Element-ui整合editor.md编辑器</title>
    <link href="undefined2019/11/06/vue-element-ui%E6%95%B4%E5%90%88editor-md%E7%BC%96%E8%BE%91%E5%99%A8/"/>
    <url>2019/11/06/vue-element-ui%E6%95%B4%E5%90%88editor-md%E7%BC%96%E8%BE%91%E5%99%A8/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/category/think.jpg" srcset="undefined" alt="封面" title="封面"></p><blockquote><p>之前一直用的是editor.md 现在使用了前后台分离 所以学习了vue 然后组件写了一个vue-editor.md编辑器,不合理之处望提出，谢谢！有一个小bug图片上传有点问题 涉及到跨越，目前没有解决，希望哪位大神能改进一下</p></blockquote><h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="javascript">&lt;template&gt;  &lt;div :id=&quot;id&quot; /&gt;&lt;/template&gt;&lt;script&gt;import $ from &#39;jquery&#39;if (typeof window !== &#39;undefined&#39;) {  var $s = require(&#39;scriptjs&#39;)}export default {  name: &#39;EditorMD&#39;,  model: {    prop: &#39;content&#39;,    event: &#39;content&#39;  },  props: {    content: {      type: String,      default: &#39;&#39;    },    width: {      type: String,      default: &#39;&#39;    },    type: {      type: String,      default: &#39;editor&#39;    },    id: {      type: String,      default: function() {        return &#39;editor-md-&#39; + +new Date() + ((Math.random() * 1000).toFixed(0) + &#39;&#39;)      }    },    editorPath: {      type: String,      default: &#39;http://localhost:9999/editor.md&#39;    },    editorConfig: {      type: Object,      default() {        return {          width: this.width,          height: 530,          path: &#39;http://localhost:9999/editor.md/lib/&#39;,          saveHTMLToTextarea: true, // 保存 HTML 到 Textarea          searchReplace: true,          htmlDecode: &#39;style,script,iframe|on*&#39;, // 开启 HTML 标签解析，为了安全性，默认不开启          emoji: true,          taskList: true,          watch: true,          toolbarAutoFixed: false,          tocm: true, // Using [TOCM]          tex: true, // 开启科学公式TeX语言支持，默认关闭          flowChart: true, // 开启流程图支持，默认关闭          sequenceDiagram: true, // 开启时序/序列图支持，默认关闭,          dialogMaskBgColor: &#39;#fff&#39;, // 设置透明遮罩层的背景颜色，全局通用，默认为#fff          imageUpload: false,          imageFormats: [&#39;jpg&#39;, &#39;jpeg&#39;, &#39;gif&#39;, &#39;png&#39;, &#39;bmp&#39;, &#39;webp&#39;],          imageUploadURL: `${process.env.VUE_APP_BASE_API}/admin/article/update1`,          crossDomainUpload: true, // 开启跨越          uploadCallbackURL: &#39;&#39;,          onload: () =&gt; {          },          onchange: () =&gt; {            if (this.instance) {              this.$emit(&#39;content&#39;, this.instance.getMarkdown())            } else {              this.$emit(&#39;content&#39;, &#39;&#39;)            }          }        }      }    }  },  data() {    return {      instance: null    }  },  watch: {    content(val) {      this.$nextTick(() =&gt;        this.$emit(&#39;content&#39;, val),      $(&#39;.editormd-markdown-textarea&#39;).val(this.content)      )    }  },  mounted() {    // 加载依赖    $s([      `https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.min.js`,      `${this.editorPath}/lib/marked.min.js`,      `${this.editorPath}/lib/prettify.min.js`,      `${this.editorPath}/lib/raphael.min.js`,      `${this.editorPath}/lib/underscore.min.js`    ], () =&gt; {      console.log(&#39;finish load js&#39;)      $s(`${this.editorPath}/editormd.min.js`, () =&gt; {        this.initEditor()      })    })  },  beforeDestroy() {    if (this.instance) {      this.instance.recreate()    }  },  methods: {    initEditor() {      this.$nextTick((editorMD = window.editormd) =&gt; {        if (editorMD) {          if (this.type === &#39;editor&#39;) {            this.instance = editorMD(this.id, this.editorConfig)            this.instance.emoji = {              path: `${this.editorPath}/plugins/emoji-dialog/emoji/`,              ext: &#39;.png&#39;            }            if (this.content !== &#39;&#39;) {              $(&#39;.editormd-markdown-textarea&#39;).val(this.content)            }          } else {            this.instance = editorMD.markdownToHTML(this.id, this.editorConfig)            if (this.content !== &#39;&#39;) {              $(&#39;.editormd-markdown-textarea&#39;).val(this.content)            }          }        }      })    }  }}&lt;/script&gt;</code></pre><p><code>dynamicLoadScript.js</code></p><pre><code class="javascript">let callbacks = []function loadedTinymce() {  // to fixed https://github.com/PanJiaChen/vue-element-admin/issues/2144  // check is successfully downloaded script  return window.tinymce}const dynamicLoadScript = (src, callback) =&gt; {  const existingScript = document.getElementById(src)  const cb = callback || function() {}  if (!existingScript) {    const script = document.createElement(&#39;script&#39;)    script.src = src // src url for the third-party library being loaded.    script.id = src    document.body.appendChild(script)    callbacks.push(cb)    const onEnd = &#39;onload&#39; in script ? stdOnEnd : ieOnEnd    onEnd(script)  }  if (existingScript &amp;&amp; cb) {    if (loadedTinymce()) {      cb(null, existingScript)    } else {      callbacks.push(cb)    }  }  function stdOnEnd(script) {    script.onload = function() {      // this.onload = null here is necessary      // because even IE9 works not like others      this.onerror = this.onload = null      for (const cb of callbacks) {        cb(null, script)      }      callbacks = null    }    script.onerror = function() {      this.onerror = this.onload = null      cb(new Error(&#39;Failed to load &#39; + src), script)    }  }  function ieOnEnd(script) {    script.onreadystatechange = function() {      if (this.readyState !== &#39;complete&#39; &amp;&amp; this.readyState !== &#39;loaded&#39;) return      this.onreadystatechange = null      for (const cb of callbacks) {        cb(null, script) // there is no way to catch loading errors in IE8      }      callbacks = null    }  }}export default dynamicLoadScript</code></pre><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>前端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>js</tag>
      
      <tag>vue</tag>
      
      <tag>element ui</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>记录一下ArrayList出现All Elements Are Null的异常</title>
    <link href="undefined2019/11/06/%E8%AE%B0%E5%BD%95%E4%B8%80%E4%B8%8B%20ArrayList%20%E5%87%BA%E7%8E%B0%20All%20elements%20are%20null%20%E7%9A%84%E5%BC%82%E5%B8%B8/"/>
    <url>2019/11/06/%E8%AE%B0%E5%BD%95%E4%B8%80%E4%B8%8B%20ArrayList%20%E5%87%BA%E7%8E%B0%20All%20elements%20are%20null%20%E7%9A%84%E5%BC%82%E5%B8%B8/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/blogImg/1562842275952.jpg" srcset="undefined" alt=""></p><p>在做项目的时候，有这么一个需求，根据条件查询地址，返回前端进行显示。之前都是正常的返回数据没有异常，突然有一天再次使用时，发现数据返回异常。<br>不同条件查询数据库正常情况下都是返回null，就是没有数据，然而现在有一个条件居然返回一条数据，遇见鬼了，具体看图：<br>正常情况下：<br><img src="https://img.luoyuanxiangvip.com/blogImg/1566196864434.jpg" srcset="undefined" alt="1.png"></p><p>SQL执行：<br><img src="https://img.luoyuanxiangvip.com/blogImg/1566196908408.jpg" srcset="undefined" alt="2.png"></p><p>工具执行结果：<br><img src="https://img.luoyuanxiangvip.com/blogImg/1566197104133.jpg" srcset="undefined" alt="6.png"></p><p>然而换一个条件，情况就变了<br>异常情况：<br><img src="https://img.luoyuanxiangvip.com/blogImg/1566196979275.jpg" srcset="undefined" alt="3.png"></p><p>SQL执行：<br><img src="https://img.luoyuanxiangvip.com/blogImg/1566197012308.jpg" srcset="undefined" alt="4.png"></p><p>工具执行结果：<br><img src="https://img.luoyuanxiangvip.com/blogImg/1566197049421.jpg" srcset="undefined" alt="5.png"></p><p>这样的结果，真的是遇见鬼了，这个情况是怎么出现的也不知道，去网上查询了一下也没有这方面的说明，在这里记录一下，希望有大神能为我解答，谢谢</p><p>记录一下我自己的处理结果：<br>我的思路是：既然有null导致的结果，那么过滤一下null就ok了<br><code>lphmcs.removeAll(Collections.singleton(null));</code><br>移除所有为空的结果，问题解决了，但是没有明白出现这种情况的原因，希望有大神为我解答，谢谢</p><blockquote><p>最糟糕的是人们在生活中经常受到错误志向的阻碍而不自知，真到摆脱了那些阻碍时才能明白过来。 —— 歌德</p></blockquote><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
      <tag>spring boot</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Springboot整合shiro配置多realm</title>
    <link href="undefined2019/11/06/springboot%E6%95%B4%E5%90%88shiro%E9%85%8D%E7%BD%AE%E5%A4%9Arealm/"/>
    <url>2019/11/06/springboot%E6%95%B4%E5%90%88shiro%E9%85%8D%E7%BD%AE%E5%A4%9Arealm/</url>
    
    <content type="html"><![CDATA[<!-- build time:Wed Nov 13 2019 15:45:14 GMT+0800 (GMT+08:00) --><p><img src="https://img.luoyuanxiangvip.com/category/code.jpg" srcset="undefined" alt="封面" title="封面"></p><p>首先自定义一个DefaultUsernamepasswordToken.java，继承Shiro自带的UsernamePasswordToken，在DefaultUsernamepasswordToken中新加一个属性<br><code>private String loginType; 而后生成getter setter；</code></p><p>首先拓展shiro 中的UsernamePasswordToken：</p><pre><code class="java">/** * 功能描述 TODO * 拓展shiro 中的UsernamePasswordToken * @author luoyuanxiang * {@link UsernamePasswordUsertypeToken} * @date  2018/12/25 10:10 */public class UsernamePasswordUsertypeToken extends UsernamePasswordToken {    private static final long serialVersionUID = 1L;    /**     * 登陆状态 app用户/管理员用户     */    private String loginType;    public String getLoginType() {        return loginType;    }    public void setLoginType(String loginType) {        this.loginType = loginType;    }    public UsernamePasswordUsertypeToken(String loginName, String password,boolean rememberMe, String loginType) {        super(loginName, password,rememberMe);        this.loginType = loginType;    }}</code></pre><p>然后自定义一个DefautModularRealm.java，继承 ModularRealmAuthenticator，重写doMultiRealmAuthentication、doSingleRealmAuthentication、doAuthenticate三个方法，还需要定义一个变量，<br>用来接收传进来的多个Realm。</p><pre><code class="java">/** * 功能描述 TODO * 自定义当使用多realm时管理器 * @author luoyuanxiang * {@link DefautModularRealm} * @date  2018/12/25 10:14 */public class DefautModularRealm extends ModularRealmAuthenticator{    private Map&lt;String, Object&gt; definedRealms;    /**     * 多个realm实现     */    @Override    protected AuthenticationInfo doMultiRealmAuthentication(            Collection&lt;Realm&gt; realms, AuthenticationToken token) {        return super.doMultiRealmAuthentication(realms, token);    }    /**     * 调用单个realm执行操作     */    @Override    protected AuthenticationInfo doSingleRealmAuthentication(Realm realm,                                                             AuthenticationToken token) {        // 如果该realms不支持(不能验证)当前token        if (!realm.supports(token)) {            throw new MyException(&quot;token错误!&quot;);        }        AuthenticationInfo info = null;        try {            info = realm.getAuthenticationInfo(token);            if (info == null) {                throw new MyException(&quot;token不存在!&quot;);            }        } catch (Exception e) {            throw new MyException(&quot;系统错误!请联系管理员！&quot;);        }        return info;    }    @Override    protected AuthenticationInfo doAuthenticate(AuthenticationToken authenticationToken) {        //先判断Realm是否为空        assertRealmsConfigured();        //强转为自定义的Token        UsernamePasswordUsertypeToken token = (UsernamePasswordUsertypeToken) authenticationToken;        Realm realm = null;        if (token.getLoginType().equals(Constant.UserType.SYS_ADMIN.getUserType())) {            realm = (Realm) this.definedRealms.get(&quot;userRealm&quot;);        }        if (token.getLoginType().equals(Constant.UserType.APP_ADMIN.getUserType())) {            realm = (Realm) this.definedRealms.get(&quot;loginRealm&quot;);        }        if (realm == null) {            return null;        }        return this.doSingleRealmAuthentication(realm, authenticationToken);    }    public Map&lt;String, Object&gt; getDefinedRealms() {        return definedRealms;    }    public void setDefinedRealms(Map&lt;String, Object&gt; definedRealms) {        this.definedRealms = definedRealms;    }}</code></pre><p>这里需要注意是 其实 流程是：在给DefaultUsernamepasswordToken赋值时，将loginType设置成相应的参数，然后在doAuthenticate时判断，单个调用对应的Realm。<br>然后是配置shirconfig类：</p><pre><code class="java">/** * 功能描述 TODO * * @author luoyuanxiang * {@link ShiroConfig} * @date  2018/12/17 13:43 */@Slf4j@Configurationpublic class ShiroConfig {    @Bean(name=&quot;shiroFilter&quot;)    public ShiroFilterFactoryBean shiroFilter(SecurityManager manager) {        ShiroFilterFactoryBean bean = new ShiroFilterFactoryBean();        bean.setSecurityManager(manager);        //配置登录的url和登录成功的url        bean.setLoginUrl(&quot;/login&quot;);        bean.setSuccessUrl(&quot;/index&quot;);        //配置访问权限        LinkedHashMap&lt;String, String&gt; filterChainDefinitionMap = new LinkedHashMap&lt;&gt;();        //表示可以匿名访问        filterChainDefinitionMap.put(&quot;/sys/login&quot;,&quot;anon&quot;);        filterChainDefinitionMap.put(&quot;/captcha.jpg&quot;,&quot;anon&quot;);        filterChainDefinitionMap.put(&quot;/assets/**&quot;,&quot;anon&quot;);        filterChainDefinitionMap.put(&quot;/logout&quot;,&quot;anon&quot;);        filterChainDefinitionMap.put(&quot;/wechat/**&quot;,&quot;anon&quot;);        //表示需要认证才可以访问        filterChainDefinitionMap.put(&quot;/**&quot;, &quot;authc&quot;);        bean.setFilterChainDefinitionMap(filterChainDefinitionMap);        return bean;    }    @Bean    public ShiroDialect shiroDialect() {        return new ShiroDialect();    }    /**     * 配置核心安全事务管理器     * @return SecurityManager     */    @Bean(name=&quot;securityManager&quot;)    public SecurityManager securityManager() {        log.info(&quot;--------------shiro已经加载----------------&quot;);        DefaultWebSecurityManager securityManager = new DefaultWebSecurityManager();        //设置realm.        securityManager.setAuthenticator(userModularRealmAuthenticator());        List&lt;Realm&gt; realms = new ArrayList&lt;&gt;(2);        //添加多个Realm        realms.add(loginRealm());        realms.add(userRealm());        securityManager.setRealms(realms);        // 自定义缓存实现 使用redis        securityManager.setCacheManager(redisCacheManager());        // 自定义session管理 使用redis        securityManager.setSessionManager(sessionManager());        //注入Cookie(记住我)管理器(remenberMeManager)        securityManager.setRememberMeManager(rememberMeManager());        return securityManager;    }    /**     * cookie对象;     *     * @return SimpleCookie     */    private SimpleCookie rememberMeCookie() {        // 这个参数是cookie的名称，对应前端的checkbox的name = rememberMe        SimpleCookie simpleCookie = new SimpleCookie(&quot;rememberMe&quot;);        // 记住我cookie生效时间30天 ,单位秒。 注释掉，默认永久不过期 2018-07-15        simpleCookie.setMaxAge(2592000);        return simpleCookie;    }    /**     * cookie管理对象;记住我功能     *     * @return CookieRememberMeManager     */    private CookieRememberMeManager rememberMeManager() {        CookieRememberMeManager cookieRememberMeManager = new CookieRememberMeManager();        cookieRememberMeManager.setCookie(rememberMeCookie());        //rememberMe cookie加密的密钥 建议每个项目都不一样 默认AES算法 密钥长度(128 256 512 位)        cookieRememberMeManager.setCipherKey(Base64.decode(&quot;1QWLxg+NYmxraMoxAXu/Iw==&quot;));        return cookieRememberMeManager;    }    /**     * 配置shiro redisManager     * 使用的是shiro-redis开源插件     *     * @return RedisManager     */    @Bean    public RedisManager redisManager() {        RedisManager redisManager = new RedisManager();        redisManager.setHost(&quot;192.168.2.106&quot;);        redisManager.setPort(6379);        redisManager.setPassword(null);        // 超时 秒        redisManager.setTimeout(6000);        // 过期时间 秒 默认30天        redisManager.setExpire(2592000);        return redisManager;    }    /**     * cacheManager 缓存 redis实现     * 使用的是shiro-redis开源插件     *     * @return RedisCacheManager     */    @Bean    @DependsOn(&quot;lifecycleBeanPostProcessor&quot;)    public RedisCacheManager redisCacheManager() {        RedisCacheManager redisCacheManager = new RedisCacheManager();        redisCacheManager.setRedisManager(redisManager());        return redisCacheManager;    }    /**     * Session Manager     * 使用的是shiro-redis开源插件     */    @Bean    @DependsOn(&quot;lifecycleBeanPostProcessor&quot;)    public DefaultWebSessionManager sessionManager() {        final DefaultWebSessionManager sessionManager = new DefaultWebSessionManager();        Cookie cookie = new SimpleCookie(java.util.UUID.randomUUID() + ShiroHttpSession.DEFAULT_SESSION_ID_NAME);        sessionManager.setSessionIdCookie(cookie);        sessionManager.setGlobalSessionTimeout(3600000);        sessionManager.setDeleteInvalidSessions(true);        sessionManager.setSessionValidationSchedulerEnabled(true);        sessionManager.setSessionDAO(redisSessionDAO());        return sessionManager;    }    /**     * RedisSessionDAO shiro sessionDao层的实现 通过redis     * 使用的是shiro-redis开源插件     */    @Bean    public RedisSessionDAO redisSessionDAO() {        RedisSessionDAO redisSessionDAO = new RedisSessionDAO();        redisSessionDAO.setRedisManager(redisManager());        return redisSessionDAO;    }    /**     * 配置自定义的权限登录器 pc登陆     * @return UserRealm     */    @Bean    @DependsOn(&quot;lifecycleBeanPostProcessor&quot;)    public UserRealm userRealm() {        return new UserRealm();    }    /**     * 配置自定义的权限登录器 app登陆     * @return LoginRealm     */    @Bean    @DependsOn(&quot;lifecycleBeanPostProcessor&quot;)    public LoginRealm loginRealm() {        return new LoginRealm();    }    /**     * 配置自定义当使用多realm时管理器     * */    @Bean    public ModularRealmAuthenticator userModularRealmAuthenticator(){        DefautModularRealm modularRealmAuthenticator = new DefautModularRealm();        modularRealmAuthenticator.setAuthenticationStrategy(new AtLeastOneSuccessfulStrategy());        Map&lt;String, Object&gt; definedRealms = new HashMap&lt;&gt;(2);        definedRealms.put(&quot;loginRealm&quot;,loginRealm());        definedRealms.put(&quot;userRealm&quot;,userRealm());        modularRealmAuthenticator.setDefinedRealms(definedRealms);        return modularRealmAuthenticator;    }    @Bean(name = &quot;lifecycleBeanPostProcessor&quot;)    public LifecycleBeanPostProcessor lifecycleBeanPostProcessor(){        return new LifecycleBeanPostProcessor();    }    @Bean    @ConditionalOnMissingBean    public DefaultAdvisorAutoProxyCreator defaultAdvisorAutoProxyCreator(){        DefaultAdvisorAutoProxyCreator creator = new DefaultAdvisorAutoProxyCreator();        creator.setProxyTargetClass(true);        return creator;    }    @Bean    public AuthorizationAttributeSourceAdvisor authorizationAttributeSourceAdvisor() {        AuthorizationAttributeSourceAdvisor advisor = new AuthorizationAttributeSourceAdvisor();        advisor.setSecurityManager(securityManager());        return advisor;    }}</code></pre><p>最后就是配置realm验证了：app登陆验证<code>LoginRealm</code>和系统登陆验证<code>UserRealm</code> 其中的内容就不一一赘述了和之前的配置完全一样<br>到此就可以实现多用户登陆验证了</p><!-- rebuild by neat -->]]></content>
    
    
    <categories>
      
      <category>后端技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
      <tag>spring boot</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>